# TODO — `Vector legacy` System Diagnostic

> **Status:** Plan phase. Nothing in this document has been fixed yet.
> **Method:** Full read of the backend, three parallel deep audits (backend / frontend / contract-infra), plus toolchain runs (`tsc`, `eslint`, `npm audit`, git-history archaeology).
> **Scope:** ~190 distinct defects. This system is **legacy headed for a rewrite**, so every entry carries a disposition tag.

**Disposition legend**
`[CARRY FORWARD]` — the concept is sound; keep it in the rewrite, fix it here if cheap.
`[REDESIGN]` — the concept is right but the implementation is structurally wrong; must be rethought.
`[ABANDON]` — dead, duplicated, or wrong-headed; delete rather than port.

**Verified toolchain state**

| Check | Result |
|---|---|
| TypeScript typecheck | Passes — but only because `skipLibCheck` hides a broken source file |
| ESLint | **Fails — 17 errors, 17 warnings** |
| npm audit | **18 vulnerabilities — 1 critical, 13 high** |
| Clean-clone install + build | **Fails** — see FE-C1 |
| Tests | **None exist anywhere** |
| CI | **None** — no workflow directory |
| Migrations | **None** — table auto-creation only |

**The four root causes.** Most individual entries below are symptoms of these:

1. **The access-control model is decorative.** The hierarchy field that every permission check reads is written by the seed script and by no application code path.
2. **Nothing enforces the API contract.** The checked-in API spec is a stale, authoritative-looking artifact that nobody regenerates.
3. **A revert removed infrastructure that was never restored** — including 704 lines of tests it had no reason to touch.
4. **Dead code is load-bearing.** The repo's build break is caused by a component that nothing imports.

---

## 1 · Security & Access Control

### Critical

- [V] **SEC-C1 · Unauthenticated registration grants the first caller MASTER** — [users.py:14-20](backend/routers/users.py#L14-L20) · `[REDESIGN]`
  **Evidence:** The route decorator carries no authentication dependency, and the role is assigned as "master" when the user count is zero.
  **Why it matters:** On any fresh or wiped deployment, the first anonymous caller on the network becomes the system administrator with full-fleet visibility and role-assignment rights. This is unauthenticated total takeover, not privilege escalation.
  **Fix:** Require authentication and the role-assignment permission on this route. Remove the count-based role branch entirely and bootstrap the initial administrator out-of-band through an environment-gated command that is not reachable over HTTP.

- [V] **SEC-C2 · System initialization endpoint is unauthenticated** — [setup.py:13-14](backend/routers/setup.py#L13-L14) · `[REDESIGN]`
  **Evidence:** Besides login and the root endpoint, this is the only route with no current-user dependency. It seeds the Master profile that carries role-assignment and view-all rights.
  **Why it matters:** It completes the SEC-C1 takeover chain — an anonymous caller can create the privileged profile and then claim it.
  **Fix:** Gate behind the same out-of-band bootstrap path as SEC-C1, or remove the endpoint and fold initialization into the seed/migration flow.

- [V] **SEC-C3 · Any authenticated user can set any equipment to any status** — [verifications.py:23-55](backend/routers/verifications.py#L23-L55) · `[REDESIGN]`
  **Evidence:** Equipment is resolved by ID with an existence check only — no ownership, holder, or unit check — and the reported status is a free-form string written straight onto the record.
  **Why it matters:** Textbook IDOR on the most sensitive write in the system. Any soldier can iterate IDs and rewrite the status of every asset in the force, corrupting the readiness metric that command reporting depends on. Arbitrary strings also poison the analytics counter, which matches on one exact value.
  **Fix:** Apply the same scoping the equipment router uses, constrain the status to a shared enumerated type, and require that the caller holds or administers the item.

- [V] **SEC-C4 · Every seeded account, including MASTER, shares one password** — [seed_data.py:135](backend/seed_data.py#L135) · `[ABANDON]`
  **Evidence:** A single hashed literal is reused for all seven demo users across every privilege level.
  **Why it matters:** Wherever the seed has ever run, a known-credential administrator backdoor exists.
  **Fix:** Generate per-account random passwords at seed time and print them once; refuse to seed outside an explicitly flagged local environment. Do not carry this pattern into the rewrite.

### High

- [V] **SEC-H1 · The hierarchy model is inert for all real data** — [equipment.py:31](backend/routers/equipment.py#L31), [users.py:26-35](backend/routers/users.py#L26-L35), [equipment.py:99](backend/routers/equipment.py#L99) · `[REDESIGN]`
  **Evidence:** The hierarchy field every scoping query reads is populated only by the seed script. User registration collects battalion and company but never maps them onto a hierarchy path; equipment creation sets no hierarchy at all.
  **Why it matters:** This is the single most consequential defect. For all non-seed data the field is null, prefix matching against null excludes the row, and every non-master user silently falls through to seeing only what they personally hold — while equipment created through the API becomes invisible to everyone but MASTER. The security model demonstrated on seed data does not exist in the running product.
  **Fix:** Decide the rewrite question at the end of this document first. As an interim repair, populate the hierarchy on both user and equipment creation and backfill existing rows.
  **Sub-document:** the rewrite question resolved to *replace*, so this item is being executed as a model replacement across 13 entries — see [TODO-SEC-H1.md](TODO-SEC-H1.md). That work also closes SEC-H2, SEC-H4, DATA-H9, DATA-M15, DATA-M16 and DATA-M17.
  **Closed at H1-13.** All 13 entries in `TODO-SEC-H1.md` are complete. The materialized-path model — `unit_hierarchy`, `unit_path`, `battalion`, `company` as authorization inputs — is gone; every scoping and authority decision now resolves through the group DAG (`Group`/`GroupEdge`/`GroupClosure`), `GroupMembership`, and `Grant`-backed `Capability` checks (`may()`/`require()`/`require_global()`). `SYSTEM_MAP.md` is synced to match throughout, not just in its access-control section.

- [V] **SEC-H2 · Unanchored prefix matching leaks across sibling units** — [equipment.py:41-46](backend/routers/equipment.py#L41-L46), [reports.py:39-44](backend/routers/reports.py#L39-L44) · `[REDESIGN]`
  **Evidence:** Battalion scoping does a string prefix match on the materialized path with no trailing-separator anchor.
  **Why it matters:** A commander of a single-digit battalion path also matches every multi-digit sibling path sharing that prefix, so they see units they have no authority over. Real cross-unit disclosure, not a theoretical one.
  **Fix:** Anchor the comparison on a path separator, and handle the exact-match case explicitly. Fix once in the shared helper proposed in DATA-H9, not twice.
  **Closed at H1-13, confirmed by grep.** There is no prefix matching left to anchor — `equipment.py` and `reports.py` both call the single shared `dependencies.scope_equipment_query()` / `scope_equipment_derived_query()` helpers, which resolve `extent(user, VIEW)` over the group closure rather than comparing path strings.

- [V] **SEC-H3 · Profile name strings override the permission matrix** — [equipment.py:125-128](backend/routers/equipment.py#L125-L128), duplicated at [equipment.py:155-158](backend/routers/equipment.py#L155-L158) · `[ABANDON]`
  **Evidence:** Authorization is granted if the profile's *name* appears in a hardcoded list, in addition to the boolean permission check.
  **Why it matters:** One profile in that list is seeded with the corresponding permission set to false — so a documented, deliberate denial is silently overridden by a string comparison. Renaming a profile row changes who can transfer equipment. Two similarly-named profiles are excluded with no stated rationale.
  **Fix:** Delete the name allowlist. Authorization must read the permission columns only.
  **Closed, found stale during a post-H1-13 audit.** No profile-name allowlist exists anywhere in `equipment.py` — confirmed by grep. `authz.require(db, current_user.id, Capability.TRANSFER, ...)` is the only check on both routes now. This was closed somewhere in the H1-8/H1-10 window (`seed_data.py`'s own comment on `TRANSFER` placement says as much: "the old route granted transfer rights to any profile whose NAME appeared in a hardcoded allowlist... there is no allowlist to disagree with now") but never had its checkbox flipped.

- [V] **SEC-H4 · Six declared permissions are enforced by no route** — [models.py:217-233](backend/models.py#L217-L233) · `[REDESIGN]`
  **Evidence:** Permissions covering equipment assignment, defect marking, item and location management, and report generation appear only in the model and the seed script. No router consults them.
  **Why it matters:** The most dangerous kind of false assurance — a security matrix that is documented, seeded, and displayed in the UI, but not enforced. Two of them guard capabilities that have no endpoints at all.
  **Fix:** For each, either wire it into the corresponding route or delete the column. Do not leave unenforced permissions visible to operators.
  **Closed at H1-12.** `Profile` and its 18 booleans are deleted along with the `profiles` table — not wired in, removed, since H1-10 had already replaced every one of them with a grant. The frontend controls that still read the flags (`EquipmentPage.tsx`'s transfer/assign gate, `MaintenancePage.tsx`'s fix gate, `AdminPanel.tsx`'s profile picker) are gone or repurposed with them, so the display half of the false assurance is gone too, not just the schema.

- [V] **SEC-H5 · Four endpoints apply no scoping whatsoever** — [analytics.py:16-17](backend/routers/analytics.py#L16-L17), [reports.py:90-110](backend/routers/reports.py#L90-L110), [maintenance.py:14](backend/routers/maintenance.py#L14), [users.py:67-74](backend/routers/users.py#L67-L74) · `[REDESIGN]`
  **Evidence:** Readiness counts every asset force-wide; the movement report returns all transaction logs; the ticket list returns every ticket; the user search returns the full personnel roster including military ID numbers and each user's permission matrix.
  **Why it matters:** Every other read path applies the scoping cascade. These four leak total force inventory, readiness posture, logistics movements, and the personnel directory to any authenticated private.
  **Fix:** Apply the shared scoping helper from DATA-H9 to all four.
  **Closed at H1-10.5.** Three scope through the equipment they derive from (`scope_equipment_query`); the roster needed a new `dependencies.scope_user_query`, since a person is in a group by many-to-many membership rather than by a column. All four are *scoped*, not gated on a capability — a listing answers what you may see, not whether you are an administrator.
  **Confirmed during H1-9, deliberately left here:** `GET /tickets/` ([maintenance.py:14](backend/routers/maintenance.py#L14)) returns *every* `MaintenanceLog` row in the force to any authenticated user, with the equipment name and fault type joined in, so a private reads the whole fleet's fault history and — via `status_filter` — its current unserviceable list. H1-9 cut the two write routes in the same file onto the group model and did **not** touch this one: it is listing scope rather than a raw-id lookup, and its fix is the same shared helper its three siblings need, applied once. Note the write paths being scoped does not narrow this at all — the leak is the list itself, not the ids in it.
  **Confirmed during H1-10, deliberately left here:** `GET /users` ([users.py:83](backend/routers/users.py#L83)) has **no gate of any kind** — not a role check, not a profile flag, nothing beyond authentication. It returns the full personnel roster with `personal_number` (the military ID) and, via `UserResponse`, each user's entire embedded permission matrix. `q` searches name and ID across the whole force; without `q` it still returns 50 rows. H1-10 introduced `MANAGE_PERSONNEL` and applied it to the three *write* routes beside this one without applying it here, on the same reasoning as `/tickets/`: this is listing scope, and the right fix is the shared helper all four siblings need rather than a fourth bespoke gate. Worth noting the write routes were the ones with a check and this one never had any, so it is the least defended endpoint of the four and the only one leaking the permission model itself.

- [V] **SEC-H6 · IDOR on every equipment write path** — [equipment.py:133](backend/routers/equipment.py#L133), [equipment.py:169](backend/routers/equipment.py#L169), [maintenance.py:47](backend/routers/maintenance.py#L47), [maintenance.py:90](backend/routers/maintenance.py#L90), [verifications.py:79](backend/routers/verifications.py#L79), [verifications.py:108](backend/routers/verifications.py#L108) · `[REDESIGN]`
  **Evidence:** Each resolves the target purely by ID. Coarse permission booleans are checked in some cases; unit scope is checked in none.
  **Why it matters:** A company-level actor can reassign a brigade commander's equipment to themselves. Any soldier can mark the entire fleet malfunctioning by iterating IDs — a denial-of-readiness attack that also corrupts analytics. Verification and status history for any asset are readable by anyone.
  **Fix:** Add a single ownership-and-scope guard applied uniformly across all six.
  **Closed, found stale during a post-H1-13 audit.** All six sites confirmed by grep to resolve through `get_scoped_equipment_or_404` and then gate on `authz.require(...)` — `assign_owner`, `transfer_equipment` (`equipment.py`), `report_fault`, `fix_equipment` (`maintenance.py`), `create_verification`, and the two verification-history reads (`verifications.py`). This is the single ownership-and-scope guard the fix called for, built as H1-9's `get_scoped_equipment_or_404`; the checkbox was never flipped when that work landed.

- [V] **SEC-H7 · Equipment creation has no permission check** — [equipment.py:87-92](backend/routers/equipment.py#L87-L92) · `[CARRY FORWARD]`
  **Evidence:** Unlike the assign and transfer routes, this one checks nothing beyond authentication, and it also auto-creates catalog entries from arbitrary user text.
  **Why it matters:** Any user can inject unbounded rows into both the equipment and catalog tables, bypassing the two permission flags that exist for precisely this.
  **Fix:** Enforce the item-creation and category-creation permissions separately, since they are distinct rights.
  **Closed, found stale during a post-H1-13 audit.** `create_equipment` now gates on `authz.require(db, current_user.id, Capability.CREATE_EQUIPMENT, group_id)`. The catalog-auto-create half of the original evidence (arbitrary user text creating catalog rows) is unchanged and is tracked separately as DATA-M6, which this entry's fix language folded in but the catalog concern was never this ticket's title.

- [V] **SEC-H8 · Verification routes skip the active-duty check** — [verifications.py:20](backend/routers/verifications.py#L20), [:76](backend/routers/verifications.py#L76), [:105](backend/routers/verifications.py#L105) · `[CARRY FORWARD]`
  **Evidence:** These three routes depend on the plain current-user resolver; every other router uses the active-duty variant.
  **Why it matters:** Discharged or deactivated personnel retain full access to submit verifications and read audit history — including the arbitrary-status write in SEC-C3.
  **Fix:** Switch to the active-duty dependency, and additionally reject inactive users at login rather than after the fact.
  **Closed at H1-10.5.** Both halves. The two remaining reads moved to `get_current_active_user`, and `login_for_access_token` now refuses an inactive account with the same 401 and the same wording as a bad password — a distinct code would make the form an oracle for which military IDs have been deactivated. The login half is the one that closes the *class* rather than the instances: a per-route check still issues the credential first.

- [ ] **SEC-H9 · Session token and a trusted user object live in browser local storage** — [auth.service.ts:31](frontend/src/services/auth.service.ts#L31), [:52](frontend/src/services/auth.service.ts#L52), [:61-63](frontend/src/services/auth.service.ts#L61-L63) · `[REDESIGN]`
  **Evidence:** The bearer token and the full serialized user object are written to local storage. The cached object is parsed with no validation and no error handling, then read back to drive UI permission decisions.
  **Why it matters:** Any cross-site scripting or compromised dependency exfiltrates a bearer token for a military logistics system. Separately, a user can edit their cached role in devtools and reload to reveal the admin interface. Malformed cached JSON throws inside a mount effect and, with no error boundary anywhere, blanks the application.
  **Fix:** Move the token to a same-site, http-only cookie or hold it in memory. Never persist the user object — always derive identity from the server. Add an error boundary.

- [ ] **SEC-H10 · The admin route is registered for every authenticated user** — [App.tsx:37](frontend/src/App.tsx#L37), [AppShell.tsx:76-79](frontend/src/components/layout/AppShell.tsx#L76-L79) · `[CARRY FORWARD]`
  **Evidence:** Only the navigation *button* is filtered by role; the route itself has no guard, so typing the path renders the panel.
  **Why it matters:** The frontend conveys an authorization boundary that does not exist. The same hide-the-button pattern is used on the equipment and maintenance pages. Real enforcement rests entirely on the backend — which, per SEC-H5 and SEC-H6, frequently does not enforce it either.
  **Fix:** Add a route-level role guard, and treat all client-side role checks as cosmetic only.
  **Widened by H1-8 through H1-10, confirmed at H1-10.5.** The client-side checks are no longer merely cosmetic — they now disagree with the backend outright. [EquipmentPage.tsx:403](frontend/src/features/equipment/components/EquipmentPage.tsx#L403) gates on `can_change_assignment_others || role === 'master'` and [MaintenancePage.tsx:85](frontend/src/features/maintenance/components/MaintenancePage.tsx#L85) on `role === 'master' || can_change_maintenance_status`. **No router reads any of those three.** So the UI both offers actions the backend refuses (a profile-flag holder with no grant) and hides actions it would permit (a grant holder whose profile flag is false) — SEC-H4's shape in both directions at once. Fixing it properly needs an endpoint exposing the caller's *capabilities*, since the frontend currently has no way to ask; that is a feature rather than debt cleanup, which is why H1-10.5 recorded it here instead of half-fixing it.
  **Narrowed at H1-12, still open.** `role`/`Profile` are gone, so the three specific mismatched checks above are gone with them — `AppShell.tsx`'s nav filter, `EquipmentPage.tsx`'s transfer/assign gate and `MaintenancePage.tsx`'s fix gate all now render unconditionally rather than checking data that no longer exists. That closes the *disagreement* (the UI no longer claims a boundary the backend contradicts), but not the ticket: nothing client-side gates these affordances at all now, so an unauthorized visitor sees every control and is refused only on click. The capabilities-endpoint fix is unchanged and still the real close.

### Medium

- [V] **SEC-M1 · Role promotion accepts an unvalidated free-form string** — [schemas.py:55](backend/schemas.py#L55) → [users.py:47](backend/routers/users.py#L47) · `[CARRY FORWARD]` — *Evidence:* No enumerated type or validator; the value is written directly to the role column. *Why:* Every role comparison in the codebase is lowercase and case-sensitive, so a capitalized or misspelled value produces an account that passes no check anywhere; nothing prevents demoting the last administrator and permanently locking the system. *Fix:* Constrain to an enumerated type and refuse to remove the final administrator.
  **Closed at H1-12.** `promote_user` (`PUT /users/promote`) and `role` are both deleted — there is no free-form string left to validate, and no route left to accept one. Authority is grants, gated by `MANAGE_PERSONNEL`, revocable and auditable.
- [ ] **SEC-M2 · Anonymous registrants control their own duty status; role is accepted then ignored** — [users.py:32](backend/routers/users.py#L32), [schemas.py:32](backend/schemas.py#L32) · `[CARRY FORWARD]` — *Evidence:* The active-duty flag is honored from client input; the role field is inherited into the create schema, advertised in the public API spec, then silently overwritten server-side. *Why:* The first is a real self-granted attribute; the second advertises a privilege-escalation vector that any future refactor to bulk attribute assignment would open for real. *Fix:* Drop both fields from the creation schema.
  **Half-closed at H1-12.** `role` is gone from `UserCreate` along with the rest of `UserBase` — nothing is advertised then overwritten, because there is no field to advertise. `is_active_duty` is untouched and still client-controlled at creation; that half stays open.
- [ ] **SEC-M3 · Login has no rate limiting, lockout, or failure logging** — [auth.py:15](backend/routers/auth.py#L15) · `[CARRY FORWARD]` — *Evidence:* No throttling middleware exists anywhere in the tree. *Why:* Unlimited guessing against personal identification numbers, which in this domain are short and enumerable. *Fix:* Add per-account and per-address throttling with lockout and audit logging.
- [ ] **SEC-M4 · Login timing oracle enables user enumeration** — [auth.py:16-17](backend/routers/auth.py#L16-L17) · `[CARRY FORWARD]` — *Evidence:* Short-circuit evaluation means password hashing never runs for a nonexistent user. *Why:* The error text is correctly uniform, but the response time is not — roughly a millisecond versus a few hundred. *Fix:* Always perform a hash comparison against a dummy value when the user is absent.
- [ ] **SEC-M5 · Tokens cannot be revoked, and the two expiry constants disagree** — [security.py:17](backend/security.py#L17) vs [security.py:34](backend/security.py#L34), [security.py:29-38](backend/security.py#L29-L38) · `[REDESIGN]` — *Evidence:* The named constant says thirty minutes while the fallback default says fifteen; the token carries no unique identifier, audience, or issuer. *Why:* A demoted or compromised user retains full privileges for the entire window with no way to intervene; any caller omitting the explicit lifetime silently gets half the intended one. *Fix:* Single source of truth for the lifetime, add a token identifier plus a revocation list, and bind audience and issuer.
- [ ] **SEC-M12 · `MANAGE_CATALOG` folds two distinct rights into one verb** — [enums.py](backend/enums.py), [setup.py](backend/routers/setup.py) · `[CARRY FORWARD]` — *Evidence:* `profiles.py` carries `can_add_category` and `can_remove_category` as separate columns; H1-10 mapped both onto the single `MANAGE_CATALOG` verb because their holders coincide exactly (Master and Brigade Tech Commander), and two verbs with identical tables on day one would be a declared distinction nothing enforces — SEC-H4's shape. *Why:* Deleting a fault type is destructive and additive creation is not, so the rights are not actually the same right; the fold is safe only while the two columns agree. *Fix:* Split into `MANAGE_CATALOG` and a removal verb the moment any profile holds one without the other, and gate `delete_fault_type` on the narrower one. Deferred deliberately at H1-10.5.
- [ ] **SEC-M6 · Interactive API documentation is anonymously exposed** — [main.py:36](backend/main.py#L36) · `[CARRY FORWARD]` — *Evidence:* The application is constructed without disabling or protecting the documentation routes. *Why:* The complete API surface of a military inventory system is enumerable without credentials. *Fix:* Disable in non-development environments or place behind authentication.
- [ ] **SEC-M7 · No password policy** — [schemas.py:37](backend/schemas.py#L37) · `[CARRY FORWARD]` — *Evidence:* The password field carries no length or complexity constraint. *Why:* A single character is an acceptable administrator password. *Fix:* Enforce minimum length and complexity at the schema layer.
- [ ] **SEC-M8 · Internal exception text is returned to the client** — [equipment.py:213-216](backend/routers/equipment.py#L213-L216) · `[CARRY FORWARD]` — *Evidence:* The caught exception is interpolated into the response detail. *Why:* Leaks table names, constraint names, and sometimes parameter values. *Fix:* Log internally, return a generic message.
- [ ] **SEC-M9 · Cross-origin configuration is hardcoded and over-permissive** — [main.py:39-52](backend/main.py#L39-L52) · `[CARRY FORWARD]` — *Evidence:* Origins are an explicit localhost list (correctly not a wildcard), but credentials are allowed alongside wildcard methods and headers, and the list requires a code change to deploy anywhere. *Why:* Credential allowance is unnecessary for bearer-token auth and widens the surface; the hardcoding creates strong pressure to "temporarily" wildcard the origins. *Fix:* Drive origins from configuration and disable credential allowance.
- [ ] **SEC-M10 · No security middleware or response headers** — [main.py](backend/main.py) · `[CARRY FORWARD]` — *Evidence:* No trusted-host, transport-security, framing, content-type, or content-security headers; no request correlation identifiers. *Why:* Standard defense-in-depth is entirely absent, and there is no way to correlate an audit event to a request. *Fix:* Add the standard middleware stack and structured request logging.
- [ ] **SEC-M11 · Search input is interpolated into query strings unencoded** — [AdminPanel.tsx:47](frontend/src/features/dashboard/components/AdminPanel.tsx#L47), [EquipmentPage.tsx:130](frontend/src/features/equipment/components/EquipmentPage.tsx#L130), [:253](frontend/src/features/equipment/components/EquipmentPage.tsx#L253) · `[CARRY FORWARD]` — *Evidence:* Template interpolation directly into the URL; the dead service layer does this correctly with parameter objects. *Why:* Ampersands and equals signs inject additional parameters; plus signs silently become spaces. *Fix:* Pass parameters through the HTTP client's parameter serialization.
- [ ] **SEC-M12 · Spreadsheet formula injection in the report export** — [GeneralReportPage.tsx:124-127](frontend/src/features/reports/components/GeneralReportPage.tsx#L124-L127) · `[CARRY FORWARD]` — *Evidence:* Quotes are escaped, but leading equals, plus, minus, at-sign, tab, and carriage return are not neutralized. *Why:* A server-stored location or serial value crafted as a formula executes when the export is opened in a spreadsheet application. *Fix:* Prefix any cell beginning with a formula trigger character.
- [ ] **SEC-M13 · A debug component leaks backend internals on the public login page** — [App.tsx:99-101](frontend/src/App.tsx#L99-L101), [ConnectionTest.tsx:9](frontend/src/components/shared/ConnectionTest.tsx#L9) · `[ABANDON]` — *Evidence:* Rendered unauthenticated; displays the hardcoded backend target and raw transport error text, and logs responses to the console. *Why:* Discloses internal addressing and error detail to anyone reaching the login screen. *Fix:* Delete the component.
- [ ] **SEC-M14 · Fault-type creation is unguarded and self-approving** — [setup.py:52-70](backend/routers/setup.py#L52-L70), [:61](backend/routers/setup.py#L61), [:78](backend/routers/setup.py#L78) · `[REDESIGN]` — *Evidence:* The category permission decides only whether the new row is marked pending, never whether the write is allowed; the same permission also grants approval. *Why:* Any user can insert unbounded rows, and the approval workflow provides no separation of duties since anyone who can approve already auto-approves. *Fix:* Gate the write on the permission and require a distinct approver role.
- [ ] **SEC-M15 · The repository bind-mount reinjects secrets that the build context excludes** — [docker-compose.yml:24-25](docker-compose.yml#L24-L25) · `[REDESIGN]` — *Evidence:* The image build correctly excludes the environment file, but the whole working tree is mounted over the application directory at run time. *Why:* The exclusion is nullified — the environment file, version-control metadata, and any local database file are all present inside the running container. *Fix:* Mount only source paths in development and mount nothing in production.
- [ ] **SEC-M16 · Identity and credential columns are nullable** — [models.py:17-18](backend/models.py#L17-L18) · `[CARRY FORWARD]` — *Evidence:* Neither the personal number nor the password hash is marked non-nullable. *Why:* The database permits unlimited null identity values under the uniqueness constraint, and a null credential causes the hashing library to raise inside the login route — turning a failed login into a server error. *Fix:* Mark both non-nullable and guard the login path.
- [ ] **SEC-M17 · Search wildcards are not escaped** — [users.py:71](backend/routers/users.py#L71), [reports.py:58](backend/routers/reports.py#L58), [equipment.py:60-64](backend/routers/equipment.py#L60-L64) · `[CARRY FORWARD]` — *Evidence:* User input is interpolated into pattern-match expressions without escaping wildcard characters. Parameterized, so **not** injection. *Why:* A single wildcard character returns the entire table; underscore-heavy input forces pathological scans. *Fix:* Escape wildcard characters before matching.

### Low

- [ ] **SEC-L1 · No token expiry check or refresh on the client** — [auth.service.ts:68-70](frontend/src/services/auth.service.ts#L68-L70) · `[REDESIGN]` — *Evidence:* Authentication state is mere presence of a stored token; the expiry claim is never decoded. *Why:* An expired session renders the authenticated shell, then every child request fails and the page reloads or redirects. *Fix:* Decode expiry client-side and add a refresh flow.
- [ ] **SEC-L2 · A failed login triggers the global session-expiry handler** — [auth.service.ts:23](frontend/src/services/auth.service.ts#L23) · `[CARRY FORWARD]` — *Evidence:* The login call routes through the interceptor that wipes storage on rejection. *Why:* Works only incidentally, because a path check happens to prevent a redirect loop. *Fix:* Exclude the login route from the interceptor explicitly.
- [V] **SEC-L3 · Token payload carries no role; the field modelling it is permanently dead** — [auth.py:26](backend/routers/auth.py#L26), [schemas.py:18](backend/schemas.py#L18) · `[ABANDON]` — *Evidence:* Only the subject claim is issued; the role field on the token data model is never populated. *Why:* A reader could mistakenly trust it; every request re-queries the database instead. *Fix:* Delete the field or populate it deliberately.
  **Closed, found stale during a post-H1-13 audit.** `TokenData` now declares only `personal_number` — the dead `role` field is gone, confirmed by reading `schemas.py` directly. This is the "delete the field" half of the named fix, landed as a byproduct of H1-12's schema cleanup with no dedicated checkbox flip at the time.
- [ ] **SEC-L4 · Password hashing has no explicit cost factor and truncates silently** — [security.py:20](backend/security.py#L20) · `[CARRY FORWARD]` — *Evidence:* The context is constructed without a rounds setting. *Why:* Cost is whatever the floating library default is, and passwords beyond the algorithm's byte limit are silently truncated with no pre-hash. *Fix:* Pin the cost factor and pre-hash long inputs.
- [ ] **SEC-L5 · Administrators cannot verify equipment; inactive users get the wrong status code** — [equipment.py:228-229](backend/routers/equipment.py#L228-L229), [dependencies.py:47-48](backend/dependencies.py#L47-L48) · `[CARRY FORWARD]` — *Evidence:* Verification requires being the holder with no administrative override, and also breaks for unheld items; the inactive-user rejection returns a client-error status rather than a forbidden status. *Why:* Commanders cannot perform spot checks; the wrong status misleads the client interceptor, which keys on the unauthorized status. *Fix:* Add an administrative override and correct the status code.

---

## 2 · Correctness & Data Integrity

### Critical

- [V] **DATA-C1 · No migration tooling exists** — [main.py:33](backend/main.py#L33) · `[CARRY FORWARD]`
  **Evidence:** The schema is created by the ORM's create-all call at import. There is no migration framework, directory, or dependency anywhere in the repository.
  **Why it matters:** Create-all only creates *missing tables* — it never alters an existing one. Every schema fix in this document is therefore unshippable to an existing database. Two columns are already marked "NEW" in the model and will simply be absent from any pre-existing database, failing at query time. This is the gating dependency for the entire Data section.
  **Fix:** Adopt a migration framework, generate an initial migration matching the current models, and route all future schema change through it. This is the first infrastructure decision, ahead of every other item here.

- [V] **DATA-C2 · The seed script destroys the entire schema with no guard** — [seed_data.py:19-22](backend/seed_data.py#L19-L22) · `[CARRY FORWARD]`
  **Evidence:** A raw cascading schema drop and recreate runs at the top of the seed routine, against whichever database the environment points at. No environment check, no confirmation, no dry run.
  **Why it matters:** Total data loss is one documented command away — and the command is documented in the repository's own notes file. Combined with DATA-C1, this is currently the project's *only* migration strategy.
  **Fix:** Require an explicit environment flag, refuse to run against anything not marked local, and separate the destructive reset from ordinary seeding.

### High

- [ ] **DATA-H1 · Every displayed timestamp is shifted by the viewer's timezone offset** — [reports.py:85](backend/routers/reports.py#L85), [reports.py:105](backend/routers/reports.py#L105) · `[CARRY FORWARD]`
  **Evidence:** Datetime columns are declared without timezone awareness and defaulted from a naive coordinated-universal-time call, then serialized with a plain ISO conversion that emits no zone designator.
  **Why it matters:** The browser parses a zoneless timestamp as *local* time. Every time shown in the reports and daily-activity tables is wrong by the client's offset — three hours for the apparent target locale. In an accountability system, "when was this last seen" being silently wrong is a correctness failure, not a cosmetic one.
  **Fix:** Make the columns timezone-aware, store aware values, and serialize with an explicit zone designator.

- [ ] **DATA-H2 · Ticket open-dates are always empty** — [maintenance.py:37](backend/routers/maintenance.py#L37), [schemas.py:118-137](backend/schemas.py#L118-L137) · `[CARRY FORWARD]`
  **Evidence:** The response is constructed with an open-date keyword that the response schema does not declare. The validation library silently ignores unknown keywords by default, and the two date fields the schema *does* declare are never populated.
  **Why it matters:** Three-way drift — a real database column, a schema that omits it, and a frontend type that declares it required. Every maintenance ticket in the interface permanently displays a placeholder dash for its open date, while the type system asserts a guaranteed value.
  **Fix:** Add the field to the response schema under its real name, remove the two unpopulated aliases, and align the frontend type.

- [ ] **DATA-H3 · Classified items are always reported as unclassified** — [schemas.py:75](backend/schemas.py#L75) · `[CARRY FORWARD]`
  **Evidence:** The sensitivity field carries a default value, and not one of the three response-construction sites passes it.
  **Why it matters:** The real classification column is silently discarded on every response, so the API reports a constant falsehood about the sensitivity of military equipment. The field is also never set, filtered, or validated anywhere — a security control that exists only as a column.
  **Fix:** Populate it from the record, constrain it to an enumerated type, and either enforce it in scoping or delete the concept.

- [ ] **DATA-H4 · Ownership changes and fault reports write no audit record** — [equipment.py:119-144](backend/routers/equipment.py#L119-L144), [maintenance.py:41-77](backend/routers/maintenance.py#L41-L77), [maintenance.py:94](backend/routers/maintenance.py#L94) · `[REDESIGN]`
  **Evidence:** The transfer, verify, and fix routes each write a transaction log entry. Ownership assignment writes none. Fault reporting writes neither a transaction log nor a status-history row, despite a dedicated status-history table existing and being used correctly by the verification router.
  **Why it matters:** Two status-change paths exist and only one is audited; the single most auditable event — a change of custody — produces no record and never appears in the movement report. For a system whose entire purpose is accountability, the provenance chain has holes.
  **Fix:** Route every state mutation through one audit-writing helper so no path can bypass it.

- [ ] **DATA-H5 · Compliance is forgeable by paperwork alone** — [equipment.py:140](backend/routers/equipment.py#L140), [equipment.py:207](backend/routers/equipment.py#L207), [models.py:86](backend/models.py#L86) · `[REDESIGN]`
  **Evidence:** Both the assign-owner and transfer routes reset the last-verified timestamp. Separately, new equipment defaults that timestamp to the moment of creation.
  **Why it matters:** Reassigning ownership in the interface marks an item physically verified without anyone laying eyes on it, so a bulk transfer resets the whole fleet to compliant. Newly created items report compliant for their first twenty-four hours despite never having been checked, which also makes the never-verified branch of the compliance rule unreachable in practice.
  **Fix:** Only a physical verification event may advance the timestamp. New equipment must start in the unverified state.

- [V] **DATA-H6 · A not-found error is returned as a server error, with internals leaked** — [equipment.py:173-216](backend/routers/equipment.py#L173-L216) · `[CARRY FORWARD]`
  **Evidence:** The not-found exception is raised *inside* the try block, and the broad catch-all treats it as an unexpected failure, wrapping it in a server error whose detail embeds the original message.
  **Why it matters:** Clients receive a server error with a doubled message for an ordinary missing-record case, and a needless rollback is triggered. It also demonstrates the pattern that makes SEC-M8 exploitable.
  **Fix:** Move validation outside the try block, or re-raise the framework exception type before the general handler.

- [ ] **DATA-H7 · Deleting a used fault type raises an unhandled server error** — [setup.py:99-104](backend/routers/setup.py#L99-L104), [models.py:191](backend/models.py#L191) · `[CARRY FORWARD]`
  **Evidence:** No foreign key in the model declares a deletion rule, and no relationship declares a cascade. The delete route performs no dependency check.
  **Why it matters:** Deleting any fault type that has ever been used violates the constraint, surfaces as a server error with a leaked constraint name, and leaves the session dirty. This is the already-reachable instance of the broader missing-cascade problem.
  **Fix:** Add explicit deletion rules to the foreign keys and pre-check dependencies with a clear conflict response.

- [ ] **DATA-H8 · Repeated query fan-out on the hottest read paths** — [equipment.py:27](backend/routers/equipment.py#L27) with [:69-84](backend/routers/equipment.py#L69-L84), [users.py:54-61](backend/routers/users.py#L54-L61), [verifications.py:93](backend/routers/verifications.py#L93), [verifications.py:123](backend/routers/verifications.py#L123) · `[CARRY FORWARD]`
  **Evidence:** These queries perform no eager loading, then the response loop touches computed properties that each traverse a separate relationship — up to four additional round trips per row. The eager-loading helper is even imported in the equipment router and never used.
  **Why it matters:** A battalion-sized inventory costs thousands of queries per page load. The correct pattern already exists one file away in the reports router, which eager-loads properly — so this is inconsistency, not ignorance.
  **Fix:** Apply the same eager loading used by the reports router to all four sites.

- [V] **DATA-H9 · The scoping block is copy-pasted and has already diverged** — [equipment.py:29-56](backend/routers/equipment.py#L29-L56) vs [reports.py:28-54](backend/routers/reports.py#L28-L54) · `[REDESIGN]`
  **Evidence:** Roughly twenty-six lines of security-critical logic exist verbatim in two routers, already differing at one line: one falls back to a secondary hierarchy field, the other does not.
  **Why it matters:** A user with only the secondary field populated sees their unit's equipment on one endpoint and only their own items on the other — two different answers to the same authorization question. Every fix, including SEC-H2, must otherwise be applied twice.
  **Fix:** Extract one shared scoping helper and route all scoped queries through it. This is the enabling refactor for SEC-H2 and SEC-H5.
  **Closed at H1-13, confirmed by grep.** The copy-paste is gone — both call sites route through the same `dependencies.py` helpers (see SEC-H2), so there is exactly one implementation of the scoping question, not two that can diverge.

- [ ] **DATA-H10 · The application cannot be imported without a live database** — [main.py:18-33](backend/main.py#L18-L33) · `[CARRY FORWARD]`
  **Evidence:** The connection-wait loop and the schema creation call both execute at module scope, blocking up to a minute and raising on failure during import.
  **Why it matters:** **This is the direct blocker for the entire test suite** — the recoverable test fixtures import the application object. It also means an import failure is unrecoverable for the server, and multiple workers race the schema creation.
  **Fix:** Move both into a startup lifespan handler. This is the first code change of the project.

- [ ] **DATA-H11 · Missing database configuration silently downgrades to a local file** — [database.py:7](backend/database.py#L7) · `[CARRY FORWARD]`
  **Evidence:** The connection string falls back to an embedded file-based database when the environment variable is absent.
  **Why it matters:** A production misconfiguration starts the application on an ephemeral local file instead of failing loudly, and combined with the repository bind-mount that file lands in the source tree. The correct fail-loud pattern is one file away, where a missing signing key correctly raises — so this is inconsistent config discipline.
  **Fix:** Fail loudly on a missing connection string, matching the signing-key behavior.

- [ ] **DATA-H12 · Status, role, and classification are unconstrained free text** — [models.py:20](backend/models.py#L20), [:69](backend/models.py#L69), [:72](backend/models.py#L72), [:193](backend/models.py#L193), [:265-266](backend/models.py#L265-L266) · `[REDESIGN]`
  **Evidence:** All are plain string columns with no enumerated type, check constraint, or schema-level validation, and the corresponding request schemas type them as bare strings.
  **Why it matters:** A misspelled status is a valid persisted value. Since the readiness metric counts one exact literal, any typo silently degrades the reported readiness of the force. This is the enabling condition for SEC-C3.
  **Fix:** Define shared enumerated types used by both the model and the schemas, backed by database check constraints.

- [ ] **DATA-H13 · No foreign key is indexed, and none declares a deletion rule** — [models.py](backend/models.py) throughout · `[CARRY FORWARD]`
  **Evidence:** Every referencing column across all twelve tables lacks both an index and a deletion rule; no relationship declares a cascade. The heavily filtered columns — holder, status, log timestamp, ticket status and open date, history dates, and role — are likewise unindexed.
  **Why it matters:** The database does not auto-index the referencing side, so every hot query path is a sequential scan over monotonically growing tables. Deleting any user or equipment row raises a constraint violation, and orphan cleanup is impossible.
  **Fix:** Add indexes and explicit deletion rules. Blocked on DATA-C1.

### Medium

- [ ] **DATA-M1 · Falsy-zero inconsistency orphans equipment on transfer** — [equipment.py:164-174](backend/routers/equipment.py#L164-L174) · `[CARRY FORWARD]` — *Evidence:* The mutual-exclusion validation tests for explicit absence while the branch tests for truthiness. *Why:* A zero identifier passes validation, takes the location branch, and silently clears both holder and location — orphaning the item instead of erroring. *Fix:* Use explicit absence checks in both places.
- [ ] **DATA-M2 · Ownership assignment accepts a nonexistent target** — [equipment.py:137-143](backend/routers/equipment.py#L137-L143) · `[CARRY FORWARD]` — *Evidence:* The target identifier is written to two foreign key columns with no lookup; the transfer route does validate its target. *Why:* Inconsistent and crash-prone — a constraint violation surfaces as an unhandled server error. *Fix:* Validate the target, matching the transfer route.
- [ ] **DATA-M3 · Duplicate serial numbers surface as server errors** — [equipment.py:99-102](backend/routers/equipment.py#L99-L102) · `[CARRY FORWARD]` — *Evidence:* The column is unique but creation performs no pre-check and catches nothing. *Why:* An ordinary data-entry collision becomes an unhandled server error rather than a conflict response. *Fix:* Pre-check and return a conflict status.
- [ ] **DATA-M4 · Multi-step writes are non-atomic** — [equipment.py:97](backend/routers/equipment.py#L97) with [:101](backend/routers/equipment.py#L101), [maintenance.py:61](backend/routers/maintenance.py#L61) with [:76](backend/routers/maintenance.py#L76) · `[CARRY FORWARD]` — *Evidence:* Each commits a lookup-or-create step separately from the main write. *Why:* A failure in the second step leaves an orphaned catalog or fault-type row behind. *Fix:* Wrap each operation in a single transaction.
- [ ] **DATA-M5 · Repair notes and fault-type names are query parameters; notes are discarded** — [maintenance.py:79-84](backend/routers/maintenance.py#L79-L84) with [:112](backend/routers/maintenance.py#L112), [setup.py:52-56](backend/routers/setup.py#L52-L56) · `[CARRY FORWARD]` — *Evidence:* Bare scalar parameters become query parameters in this framework; a dedicated request schema exists for the fault-type case and is never used. *Why:* Repair rationale is echoed back to the caller and written to no table — permanently lost. Any client sending a proper request body for the fault type receives a validation error. *Fix:* Declare both as request bodies and persist the notes.
- [ ] **DATA-M6 · Catalog entries are auto-created from arbitrary text with no normalization** — [equipment.py:93-97](backend/routers/equipment.py#L93-L97) · `[REDESIGN]` — *Evidence:* A lookup-or-create on a free-text name, with no permission check and no case or whitespace normalization. *Why:* Three spellings of one item become three catalog rows, permanently fragmenting all inventory reporting; concurrent creates race the uniqueness constraint. *Fix:* Normalize, require the category permission, and pre-seed the catalog.
- [ ] **DATA-M7 · Reporting-status vocabulary drifts three ways** — [reports.py:74](backend/routers/reports.py#L74), [schemas.py:156](backend/schemas.py#L156) · `[CARRY FORWARD]` — *Evidence:* The route emits one vocabulary while the documenting comment on the unused schema describes a different one, and the frontend expects a third. *Fix:* One shared enumerated type across producer, schema, and consumer.
- [ ] **DATA-M8 · The reporter column in the movement report is hardcoded empty** — [reports.py:108](backend/routers/reports.py#L108) · `[CARRY FORWARD]` — *Evidence:* The value is a literal null; the log table stores the acting user, but the model declares no relationship to join back. *Why:* A column rendered in the activity table is permanently blank, so movement records cannot be attributed. *Fix:* Add the relationship and populate the field.
- [ ] **DATA-M9 · The "daily" movement report is a rolling window, not a calendar day** — [reports.py:95](backend/routers/reports.py#L95) · `[CARRY FORWARD]` — *Evidence:* A fixed twenty-four-hour subtraction from the current instant, with no date parameter. *Why:* The result set shifts continuously, straddles the local day boundary, and historical days are unreachable. *Fix:* Accept a date parameter and bound it to local calendar days.
- [ ] **DATA-M10 · Filter joins silently drop rows and are unbounded** — [reports.py:58](backend/routers/reports.py#L58), [:64](backend/routers/reports.py#L64), [:68](backend/routers/reports.py#L68), [:101](backend/routers/reports.py#L101), [maintenance.py:27](backend/routers/maintenance.py#L27), [users.py:74](backend/routers/users.py#L74) · `[CARRY FORWARD]` — *Evidence:* Inner joins are layered on top of eager-load options for the same relationships; every list endpoint fetches all rows with no pagination. *Why:* Filtering by holder silently excludes all unheld equipment — exactly the orphaned items most needing attention — and the ever-growing log table is fetched whole on each dashboard load. *Fix:* Use outer joins for optional filters and add pagination throughout.
- [ ] **DATA-M11 · Compliance is computed by two divergent implementations** — [dependencies.py:58-68](backend/dependencies.py#L58-L68) vs [models.py:133-144](backend/models.py#L133-L144) · `[ABANDON]` — *Evidence:* A helper function reimplements a model property; two response-construction sites use the helper and a third uses the property. *Why:* Two sources of truth for one business rule, already used inconsistently. *Fix:* Delete the helper; keep the model property.
- [ ] **DATA-M12 · Required response fields map to nullable columns** — [schemas.py:127](backend/schemas.py#L127), [schemas.py:43](backend/schemas.py#L43), [models.py](backend/models.py) throughout · `[CARRY FORWARD]` — *Evidence:* Ticket description and user last-seen are required in the response schemas but nullable in the database; many business-critical columns lack a non-null constraint. *Why:* One null row raises a validation error that fails the *entire list response*, not just that row. *Fix:* Add non-null constraints, or make the schema fields optional. Blocked on DATA-C1.
- [ ] **DATA-M13 · Unique-plus-nullable defeats serial number uniqueness** — [models.py:66](backend/models.py#L66), [:17](backend/models.py#L17) · `[CARRY FORWARD]` — *Evidence:* Both unique columns are also nullable. *Why:* The database permits unlimited null values under a uniqueness constraint, so uniqueness is unenforced for exactly the items most likely to be mis-entered. *Fix:* Make required, or add a partial unique constraint.
- [ ] **DATA-M14 · A defined ticket-status enumeration is entirely unused** — [models.py:295-301](backend/models.py#L295-L301) · `[ABANDON]` — *Evidence:* The column is a plain string compared against literals elsewhere; two of the enumeration's values are unreachable by any code path. *Fix:* Either adopt it as the column type or delete it.
- [V] **DATA-M15 · The role type is a plain class and four of its five members are dead** — [models.py:7-12](backend/models.py#L7-L12) · `[REDESIGN]` — *Evidence:* Not an enumeration, so it provides no validation; only the administrator member is ever consulted by an authorization check, though the others are assigned by the seed. *Why:* The role field is effectively a single boolean, while presenting as a five-level hierarchy. *Fix:* Convert to a real enumeration and either enforce the intermediate levels or remove them.
  **Closed at H1-12, by deletion rather than the suggested fix.** `UserRole` and `User.role` are both gone. Converting to a real enumeration would have kept validating a field nothing reads; authority is grants, and a role display is no longer part of the model at all.
- [V] **DATA-M16 · Three overlapping representations of unit hierarchy** — [models.py:24-27](backend/models.py#L24-L27), [:56](backend/models.py#L56), [:73](backend/models.py#L73) · `[ABANDON]` — *Evidence:* A path field, a materialized-path field with a different example format, and separate battalion and company fields — the latter settable at registration and read by no query. Equipment carries only one of the three, so they are not even joinable. *Fix:* Choose one representation in the rewrite and delete the others.
  **Closed at H1-11.** `users.unit_hierarchy`, `users.unit_path`, `equipment.unit_hierarchy` and `locations.unit_path` are gone; `equipment.group_id` is the single representation and is now `NOT NULL`. The migration backfilled `group_id` from the path strings before destroying them and refuses to run rather than stranding an item in no group. `users.battalion`/`company` — the pair the audit noted as *settable at registration and read by no query* — survive one more entry: they are the only two with a live reader, and H1-12 takes them with the rest of `UserResponse`.
- [V] **DATA-M17 · A duplicated permission column with no synchronization** — [models.py:211-212](backend/models.py#L211-L212) · `[ABANDON]` — *Evidence:* Two adjacent boolean columns whose own comments state they map to each other; one appears nowhere else in the codebase. *Why:* Permission drift waiting to happen. *Fix:* Delete the unused one.
  **Closed at H1-12.** Both columns, and the `Profile` table they sat on, are gone. Nothing left to drift.
- [ ] **DATA-M18 · Three tables and roughly seven columns are entirely dead** — [models.py:45-60](backend/models.py#L45-L60), [:243-246](backend/models.py#L243-L246), [:249-255](backend/models.py#L249-L255), [:31](backend/models.py#L31), [:162](backend/models.py#L162), [:168-169](backend/models.py#L168-L169), [:181](backend/models.py#L181) · `[ABANDON]` — *Evidence:* An analytics cache never written or read, a solution-type table never referenced, and a locations table with no endpoints — its identifier only ever set to null — plus never-written log and fault columns. The user last-seen column is set once by its default and never updated, yet is surfaced by the API as live presence data. *Why:* All are created on every startup; the presence data is actively misleading. *Fix:* Delete in the rewrite; document the locations feature as unimplemented rather than half-present.
- [ ] **DATA-M19 · The response-construction block is copy-pasted three times and has diverged** — [equipment.py:71-84](backend/routers/equipment.py#L71-L84), [:104-117](backend/routers/equipment.py#L104-L117), [users.py:55-61](backend/routers/users.py#L55-L61) · `[REDESIGN]` — *Evidence:* Thirteen identical arguments at each site; one already uses the model property where the others use the helper (see DATA-M11). *Fix:* Extract one serialization function.
- [ ] **DATA-M20 · No global exception handler; console printing instead of logging** — [main.py](backend/main.py), [main.py:25](backend/main.py#L25), [:28](backend/main.py#L28), [:70](backend/main.py#L70), [equipment.py:215](backend/routers/equipment.py#L215) · `[CARRY FORWARD]` — *Evidence:* No handler for constraint violations or unexpected errors; no logging framework anywhere; startup diagnostics use emoji console output. *Why:* Every defect in DATA-M2, M3, and H7 surfaces as a bare server error; the emoji output raises an encoding error on the documented development console; without unbuffered output the diagnostics never reach container logs at all. *Fix:* Adopt structured logging and register exception handlers.
- [ ] **DATA-M21 · Closing a ticket is keyed on equipment, not the ticket** — [MaintenancePage.tsx:114](frontend/src/features/maintenance/components/MaintenancePage.tsx#L114), [maintenance.py:97-100](backend/routers/maintenance.py#L97-L100) · `[REDESIGN]` — *Evidence:* The per-ticket button calls an equipment-keyed route that bulk-closes every open ticket for that item. *Why:* Closing one ticket silently closes its siblings, and the interface shows them as open until the next refresh. *Fix:* Make the operation ticket-scoped.

### Low

- [V] **DATA-L1 · A walrus assignment in a class base-class position** — [models.py:203](backend/models.py#L203) · `[ABANDON]` — *Evidence:* Legal but bizarre: evaluating the base-class expression binds a module-level alias for the ORM base as a side effect. *Why:* It leaks a name identical to the one the validation library uses one file over — a genuine landmine for anyone importing broadly, and almost certainly a find-and-replace artifact from a past merge. *Fix:* Delete the assignment.
  **Closed at H1-13, byproduct of H1-12.** The walrus assignment lived on `class Profile(BaseModel := Base)`; `Profile` itself is deleted, so the landmine is gone with its whole class. Confirmed via grep — zero `:=` occurrences in `models.py`.
- [ ] **DATA-L2 · Deprecated datetime and ORM-base idioms throughout** — [security.py:32-34](backend/security.py#L32-L34), [database.py:3](backend/database.py#L3), [models.py](backend/models.py) throughout · `[CARRY FORWARD]` — *Evidence:* The naive coordinated-universal-time call is deprecated in current interpreter versions and is used in roughly twenty places; the declarative base is imported from its deprecated location. *Why:* Introducing a single timezone-aware value anywhere produces a type error on subtraction. *Fix:* Migrate to timezone-aware calls and the current import path, alongside DATA-H1.
- [ ] **DATA-L3 · No connection liveness checking** — [database.py:13](backend/database.py#L13) · `[CARRY FORWARD]` — *Evidence:* The engine is created without pre-ping or pool sizing. *Why:* After a database restart the pool serves dead connections, failing the first request. *Fix:* Enable pre-ping and tune the pool.
- [ ] **DATA-L4 · Thirteen unused imports across six files, and a mid-file import** — [security.py:3](backend/security.py#L3), [equipment.py:5-6](backend/routers/equipment.py#L5-L6), [setup.py:4-9](backend/routers/setup.py#L4-L9), [models.py:296](backend/models.py#L296), others · `[CARRY FORWARD]` — *Evidence:* Includes the eager-loading helper that DATA-H8 needs, and an import at line 296 of a 301-line file. *Why:* The configured linter selects the rules that catch all of these, so it has evidently never been run against this tree. *Fix:* Run the linter and clear the findings.
- [ ] **DATA-L5 · Seed script holds an unclosed session and relies on implicit flush ordering** — [seed_data.py:13](backend/seed_data.py#L13), [:153-170](backend/seed_data.py#L153-L170) · `[CARRY FORWARD]` — *Evidence:* A session is opened at module scope and never closed; a later block reads generated identifiers that are only populated because an unrelated intervening commit happens to flush first. *Why:* Merely importing the module opens a connection; reordering two independent blocks silently produces null foreign keys. *Fix:* Scope the session properly and flush explicitly.
- [ ] **DATA-L6 · Tautological role comparisons and dead re-export aliases** — [dependencies.py:52](backend/dependencies.py#L52), [:18-22](backend/dependencies.py#L18-L22), [equipment.py:34](backend/routers/equipment.py#L34), [reports.py:32](backend/routers/reports.py#L32) · `[CARRY FORWARD]` — *Evidence:* Each comparison tests a constant against the identical literal it equals; three of five re-exported aliases are imported by nobody. *Why:* The doubled comparison signals genuine uncertainty about whether the role is an enumeration or a string — resolved by DATA-M15. *Fix:* Collapse the comparisons and delete the unused aliases.

---

## 3 · Contract Drift

### Critical

- [V] **API-C1 · The Admin Panel crashes on every user selection** — [AdminPanel.tsx:70](frontend/src/features/dashboard/components/AdminPanel.tsx#L70), [schemas.py:21-27](backend/schemas.py#L21-L27) · `[CARRY FORWARD]`
  **Evidence:** The component reads an identifier off the nested profile object using optional chaining on the *profile*, not on the identifier. The backend profile response schema does not expose an identifier at all.
  **Why it matters:** Because the backend eagerly loads the profile, the object is always present — so the optional chain never short-circuits and the code calls a method on an undefined value. Selecting any search result throws, and with no error boundary anywhere the whole application white-screens.
  **Fix:** Add the identifier to the profile response schema and guard the access.

- [V] **API-C2 · The Admin Panel's only write endpoint does not exist** — [AdminPanel.tsx:78](frontend/src/features/dashboard/components/AdminPanel.tsx#L78) · `[REDESIGN]`
  **Evidence:** The save handler calls a profile-assignment route that is present in no router. A dedicated request schema was written for it and is referenced nowhere else.
  **Why it matters:** The panel's single write operation can never succeed — it always returns not-found and surfaces a generic failure alert. Meanwhile the *real* role-promotion endpoint that does exist has no caller anywhere in the frontend. The documented "profile assignment and role promotion" feature is entirely non-functional.
  **Fix:** Decide whether to implement the endpoint or repoint the client at the existing promotion route. Record as a scoped feature decision, not a silent fix.

- [V] **API-C3 · The report export endpoint does not exist** — [ExportControls.tsx:11-14](frontend/src/features/dashboard/components/ExportControls.tsx#L11-L14) · `[REDESIGN]`
  **Evidence:** Both export buttons call a route present in no router. The revert that removed the test suite also deleted this feature's dedicated tests.
  **Why it matters:** The feature and its tests were lost together. The component is currently unreferenced, so there is no live user impact — but the documentation still advertises the capability.
  **Fix:** Either restore the endpoint alongside its recovered tests or remove the component and correct the documentation.

### High

- [ ] **API-H1 · The checked-in API specification describes a version that no longer exists** — [openapi.json:5](frontend/openapi.json#L5), [main.py:36](backend/main.py#L36) · `[ABANDON]`
  **Evidence:** The specification declares one API version while the running application declares a later one; it documents ten of the twenty-eight live paths. Eighteen endpoints — including the entire verification, transfer, history, reporting, and fault-management surface — are absent.
  **Why it matters:** A tracked, generated file that nobody regenerates is worse than no file: it is a checked-in, authoritative-looking lie, and it is the input from which the client is regenerated. It also declares security requirements on the two endpoints that are in fact unauthenticated.
  **Fix:** Stop tracking it. Generate the specification in continuous integration from the live application and fail the build on drift.

- [ ] **API-H2 · Generated client models contradict the real API in eight distinct ways** — [client/models/](frontend/src/client/models/) · `[ABANDON]`
  **Evidence:** The equipment model carries a field renamed long ago and omits seven that exist. The readiness model has all three field names wrong. The ticket model omits the two fields the interface actually renders while requiring two that are always empty. The assign-owner model requires a field the backend does not accept. The equipment-create model omits the serial number. The user models mark optional fields required, type the role as an enumeration the backend does not use, and omit both the profile and last-seen fields. The promotion model types the role as an enumeration while the backend accepts any string.
  **Why it matters:** Anyone who "migrates to the generated client" — the obvious next step for a new contributor — gets silent undefined values and broken calculations. The readiness rename in particular is documented in the project's own notes as having previously caused a server error.
  **Fix:** Delete the generated tree. Regenerate on demand in continuous integration, or hand-maintain one shared type module.

- [ ] **API-H3 · The shared profile type declares fifteen fields; the backend sends six** — [types/index.ts:19-36](frontend/src/types/index.ts#L19-L36), [schemas.py:21-27](backend/schemas.py#L21-L27) · `[CARRY FORWARD]`
  **Evidence:** Ten declared permission fields are never serialized, all typed as required booleans.
  **Why it matters:** The type system reports them as guaranteed booleans while they are undefined at runtime, so every permission gate built on them silently evaluates false. All ten exist as real columns — this is a serialization gap, not a modelling one. It is also the direct cause of API-C1.
  **Fix:** Expose the full permission set in the response schema, or narrow the frontend type to what is actually sent.

- [ ] **API-H4 · The shared inventory report type is wrong on seven of eight fields** — [types/index.ts:88-97](frontend/src/types/index.ts#L88-L97), [reports.py:77-85](backend/routers/reports.py#L77-L85) · `[CARRY FORWARD]`
  **Evidence:** Nearly every field name differs from what the route emits. The component that actually renders the report declares its own private, *correct* interface; the shared type is used only by a dead service.
  **Why it matters:** Two competing, contradictory type definitions for one endpoint, with the wrong one in the shared module where the next contributor will find it. A third, also-unused definition exists on the backend.
  **Fix:** Delete the wrong shared type and the dead service; promote the component's correct interface into the shared module.

- [ ] **API-H5 · The ticket filter parameter name differs between the generated client and the backend** — [DefaultService.ts:160-168](frontend/src/client/services/DefaultService.ts#L160-L168), [maintenance.py:16](backend/routers/maintenance.py#L16) · `[ABANDON]`
  **Evidence:** The generated client sends one parameter name; the backend expects another. The hand-written call site uses the correct one.
  **Why it matters:** The generated path would silently return *all* tickets instead of only open ones — a wrong number presented as a filtered count, with no error.
  **Fix:** Resolved by deleting the generated client (API-H2).

- [ ] **API-H6 · Nine backend endpoints have no caller, and the fault-approval workflow is unreachable** — [setup.py:43](backend/routers/setup.py#L43), [:52](backend/routers/setup.py#L52), [:72](backend/routers/setup.py#L72), [:90](backend/routers/setup.py#L90), [users.py:41](backend/routers/users.py#L41), [verifications.py:72](backend/routers/verifications.py#L72), others · `[REDESIGN]`
  **Evidence:** Fault reporting marks user-created types as pending, and the listing route returns all types regardless of pending state — while the three endpoints that would complete the approval loop have zero callers.
  **Why it matters:** A pending flag is written, exposed, and never acted upon. The approval workflow is decorative, and verification history is effectively write-only.
  **Fix:** Either wire the workflow into the interface or remove the pending concept entirely.

### Medium

- [ ] **API-M1 · Eight backend schemas are defined and never used** — [schemas.py:6](backend/schemas.py#L6), [:49](backend/schemas.py#L49), [:57](backend/schemas.py#L57), [:102](backend/schemas.py#L102), [:107-116](backend/schemas.py#L107-L116), [:139](backend/schemas.py#L139), [:149](backend/schemas.py#L149) · `[ABANDON]` — *Evidence:* Declaration-only, verified by full-tree search. Several contradict the routes they nominally describe. *Fix:* Delete, or adopt as response models per API-M2.
- [ ] **API-M2 · Fifteen endpoints return untyped structures with no response model** — [reports.py:13](backend/routers/reports.py#L13), [:90](backend/routers/reports.py#L90), [analytics.py:11](backend/routers/analytics.py#L11), [setup.py](backend/routers/setup.py) throughout, action routes across three routers · `[CARRY FORWARD]` — *Evidence:* Hand-built dictionaries with no declared schema. *Why:* No response validation and no entry in the generated specification, which is why the client has no types for them and the frontend duplicates definitions by hand. *Fix:* Declare response models throughout — this is the structural fix for the whole Contract Drift section.
- [ ] **API-M3 · Field-level type mismatches on live paths** — [schemas.py:43](backend/schemas.py#L43), [types/index.ts:57](frontend/src/types/index.ts#L57), [types/index.ts:6-17](frontend/src/types/index.ts#L6-L17) · `[CARRY FORWARD]` — *Evidence:* Last-seen is required by the backend and absent from the frontend type; the compliance union declares a fourth value the backend cannot produce; the frontend user type declares two hierarchy fields the response never returns. *Why:* The compliance case is telling — a component adds a defensive fallback for a value the type system says cannot be empty. *Fix:* Generate one shared type module from the specification.
- [ ] **API-M5 · Two routes perform one verification, under different gates** — [equipment.py:290](backend/routers/equipment.py#L290), [verifications.py:21](backend/routers/verifications.py#L21) · `[REDESIGN]` — *Evidence:* `verify_equipment_daily` (`POST /equipment/{id}/verify`) and `create_verification` (`POST /verifications/`) both stamp `last_verified_at` and both write an audit row, but the first is gated holder-only while the second is holder-or-`REPORT_STATUS`, and only the second records a reported status or a status-history entry. *Why:* Two audit paths for one real-world act, so the compliance clock can be reset by a route that captures none of the finding; and the differing gates mean the same user can be permitted one and refused the other for the same item. *Fix:* Make the daily stamp a `verification_type` on the single `/verifications/` route and retire the equipment-scoped one. This is an API change rather than a deletion — the frontend calls it from [equipment.service.ts:38](frontend/src/services/equipment.service.ts#L38) and [EquipmentPage.tsx:430](frontend/src/features/equipment/components/EquipmentPage.tsx#L430). Deferred deliberately at H1-10.5.
- [ ] **API-M4 · Four disagreeing version numbers** — [main.py:36](backend/main.py#L36), [openapi.json:5](frontend/openapi.json#L5), [package.json:4](frontend/package.json#L4), [SYSTEM_MAP.md:352-356](SYSTEM_MAP.md#L352-L356) · `[CARRY FORWARD]` — *Evidence:* Four artifacts declare four different versions. *Why:* No reliable answer to "what is deployed". *Fix:* Single version source, referenced everywhere.

---

## 4 · Frontend

### Critical

- [V] **FE-C1 · The main branch does not build from a clean clone** — [package.json](frontend/package.json), [ParticlesBackground.tsx:2-4](frontend/src/components/ui/ParticlesBackground.tsx#L2-L4) · `[ABANDON]`
  **Evidence:** The committed manifest declares **zero** particle-library dependencies; the committed component imports **three**. Verified: the component is imported by **no file in the repository**. The uncommitted working-tree change adds those three dependencies — roughly thirty transitive packages — to make a component build that nothing renders.
  **Why it matters:** Anyone cloning the default branch and installing from the lockfile fails at the typecheck step. This is the repository's headline defect and it is caused entirely by dead code. Note the login page already uses a *different* animated background, so this is a second, competing, unused system.
  **Fix:** **Delete the component.** This restores the build without adding a single dependency — strictly better than committing the work-in-progress. Discard the uncommitted manifest and lockfile changes.

### High

- [ ] **FE-H1 · Four competing HTTP layers with three base addresses and two conflicting session-expiry behaviors** — [api.ts:5](frontend/src/api.ts#L5), [lib/axios.ts:8](frontend/src/lib/axios.ts#L8), [main.tsx:7](frontend/src/main.tsx#L7), [ConnectionTest.tsx:9](frontend/src/components/shared/ConnectionTest.tsx#L9) · `[REDESIGN]`
  **Evidence:** One client runs all ten feature components with no timeout; a second, with a ten-second timeout, runs only login and bootstrap; a third is dead but bundled; a fourth is a bare call against a hardcoded address. On session expiry the first performs a full page reload while the second performs a hard navigation — and the first forgets to clear the cached user object.
  **Why it matters:** Two contradictory session-expiry behaviors in one application, and the reload path destroys unsaved form state such as a half-written fault report. Two of the four addresses ignore the configured environment variable entirely, so they break on any non-local deployment. The current-user endpoint is fetched **four times** on a single dashboard load, across two clients with different timeouts and different failure semantics.
  **Fix:** Consolidate to one client with one interceptor, one base address from configuration, and one expiry behavior.

- [ ] **FE-H2 · A server-only library is bundled into the browser and is not a declared dependency** — [client/core/request.ts:7](frontend/src/client/core/request.ts#L7) · `[ABANDON]`
  **Evidence:** The generated client imports a form-encoding library built on server stream and buffer primitives. It appears nowhere in the manifest and resolves only as a transitive dependency of the HTTP library. The application entry point imports the generated tree solely to set one unused constant, which drags the whole chain into the bundle.
  **Why it matters:** Any deduplication or major HTTP-library upgrade that hoists the transitive dependency differently breaks the build with no warning. The same file also base-64 encodes using a server-only primitive behind a type-checker suppression.
  **Fix:** Delete the generated tree and the entry-point import.

- [ ] **FE-H3 · A type-declaration file imports a symbol removed in the installed major version** — [r3f.d.ts:1](frontend/src/r3f.d.ts#L1) · `[ABANDON]`
  **Evidence:** The symbol was removed in the installed renderer's version nine. The build passes **only** because the library-check skip flag also skips declaration files inside the source directory. Every element the file re-declares already exists in the current library, so they would be duplicate-property errors if checked; one declared element is a misspelling of the real one.
  **Why it matters:** Turning off the skip flag — a normal hardening step, and one this document recommends — breaks the build immediately. The file is entirely dead in the meantime.
  **Fix:** Delete the file, then enable library checking.

- [ ] **FE-H4 · An animation loop with no cleanup, instantiated four times** — [StatsGrid.tsx:13-37](frontend/src/features/dashboard/components/StatsGrid.tsx#L13-L37) · `[CARRY FORWARD]`
  **Evidence:** The effect starts a recursive animation-frame loop and returns no cleanup function. The hook is used for four separate counters.
  **Why it matters:** Unmounting mid-animation keeps writing state to a dead component. Worse, when the ticket count resolves or the user refreshes, the target changes and a *second* loop starts while the first still runs — both writing different eased values, producing a visibly stuttering, rewinding counter. With four counters and repeated refreshes the loops accumulate.
  **Fix:** Cancel the pending frame in the effect's cleanup.

- [ ] **FE-H5 · No request cancellation anywhere — eight identified race conditions** — [AdminPanel.tsx:42-57](frontend/src/features/dashboard/components/AdminPanel.tsx#L42-L57), [EquipmentPage.tsx:126-138](frontend/src/features/equipment/components/EquipmentPage.tsx#L126-L138), [:249-261](frontend/src/features/equipment/components/EquipmentPage.tsx#L249-L261), [DailyActivityTable.tsx:74-90](frontend/src/features/dashboard/components/DailyActivityTable.tsx#L74-L90), [App.tsx:62](frontend/src/App.tsx#L62), others · `[CARRY FORWARD]`
  **Evidence:** No cancellation primitive appears anywhere in the source. The three debounced searches clear the *timer* but never abort an in-flight request.
  **Why it matters:** Typing, pausing, then typing again issues overlapping searches where the last response wins regardless of which query it answers — so the results shown may not match the text in the box. Closing a modal or navigating away mid-request writes state to unmounted components.
  **Fix:** Thread a cancellation signal through the shared client from FE-H1 and abort on cleanup.

- [ ] **FE-H6 · The lint script currently fails** — 17 errors, 17 warnings · `[CARRY FORWARD]`
  **Evidence:** Errors include an access-before-declaration in the admin panel, two impure random calls during render, four synchronous state updates inside effects, four unused variables, four unconstrained dynamic types, and two fast-refresh violations. Fifteen warnings are stale suppression directives inside the generated tree.
  **Why it matters:** Nothing enforces the script, so it has been allowed to fail. Several errors are genuine defects catalogued separately here, not style noise.
  **Fix:** Clear the errors, ignore the generated tree, and enforce a zero-warning threshold in continuous integration.

- [ ] **FE-H7 · No test infrastructure of any kind** — [package.json:6-12](frontend/package.json#L6-L12) · `[CARRY FORWARD]`
  **Evidence:** No test script, no test runner, no testing library, no test files. Also no typecheck script — type checking happens only inside the build.
  **Why it matters:** Zero coverage on an application managing military asset custody.
  **Fix:** Add a runner and testing library, plus standalone typecheck and test scripts.

- [ ] **FE-H8 · Eighteen dependency vulnerabilities, one critical and thirteen high** · `[CARRY FORWARD]`
  **Evidence:** Includes a routing-library deserialization flaw permitting unauthenticated remote code execution, a build-tool path traversal, and a template-library injection.
  **Why it matters:** The routing library is a core runtime dependency on every page.
  **Fix:** Apply the automated remediation, then re-run typecheck and lint to confirm no regression.

### Medium

- [ ] **FE-M1 · Roughly 1,300 lines of dead code** — [client/](frontend/src/client/) (23 files), [services/equipment.service.ts](frontend/src/services/equipment.service.ts), [services/reports.service.ts](frontend/src/services/reports.service.ts), [LegacyLogin.tsx](frontend/src/features/auth/components/LegacyLogin.tsx), [EquipmentTable.tsx](frontend/src/features/dashboard/components/EquipmentTable.tsx), [ExportControls.tsx](frontend/src/features/dashboard/components/ExportControls.tsx), [ParticlesBackground.tsx](frontend/src/components/ui/ParticlesBackground.tsx), [card.tsx](frontend/src/components/ui/card.tsx), [App.css](frontend/src/App.css), [r3f.d.ts](frontend/src/r3f.d.ts) · `[ABANDON]` — *Evidence:* Verified by exhaustive import search. The superseded login component still writes a session token; the superseded equipment table is still styled light-only and would be unreadable in dark mode. *Why:* It is not inert — it breaks the build (FE-C1), poisons the bundle (FE-H2), and misleads every reader about which code path is live. *Fix:* Delete wholesale.
- [ ] **FE-M2 · A data-fetching library is installed and mounted but never used** — [App.tsx:3](frontend/src/App.tsx#L3), [:17](frontend/src/App.tsx#L17), [:89](frontend/src/App.tsx#L89) · `[REDESIGN]` — *Evidence:* The provider is configured and mounted; no component calls any of its hooks. *Why:* A caching, deduplicating, retrying library sits unused while every component hand-rolls state and effects — and the current-user endpoint is fetched four times per page load (FE-H1). *Fix:* Either adopt it — which would resolve FE-H5 and much of FE-M4 — or remove it.
- [ ] **FE-M3 · Blocking alerts as the error interface, and errors swallowed to the console** — ten alert sites across five components; eight silent-swallow sites including [App.tsx:62](frontend/src/App.tsx#L62), [AdminPanel.tsx:49](frontend/src/features/dashboard/components/AdminPanel.tsx#L49), [GeneralReportPage.tsx:58-60](frontend/src/features/reports/components/GeneralReportPage.tsx#L58-L60) · `[REDESIGN]` — *Evidence:* Native alerts are used for failures and, in one case, for success. Elsewhere failures are logged and nothing is shown. *Why:* Alerts are blocking, unstyled, and left-to-right inside a right-to-left application. The silent path is worse: a failed search renders "no users found", a failed history fetch renders "no history records", and a failed report renders "no items match the current filter" — network failures presented to operators as authoritative empty results. Twenty-five console calls ship to production. *Fix:* A shared toast and inline error-state pattern; distinguish empty from failed everywhere.
- [ ] **FE-M4 · No error boundary; four incorrect effect dependency lists; three synchronous state updates in effects** — [AdminPanel.tsx:37-39](frontend/src/features/dashboard/components/AdminPanel.tsx#L37-L39), [DashboardPage.tsx:37-39](frontend/src/features/dashboard/components/DashboardPage.tsx#L37-L39), [GeneralReportPage.tsx:65-67](frontend/src/features/reports/components/GeneralReportPage.tsx#L65-L67), [EquipmentHistory.tsx:27-31](frontend/src/features/equipment/components/EquipmentHistory.tsx#L27-L31), [App.tsx:56](frontend/src/App.tsx#L56), [AppShell.tsx:61](frontend/src/components/layout/AppShell.tsx#L61), [ThemeToggle.tsx:13](frontend/src/components/ui/ThemeToggle.tsx#L13) · `[CARRY FORWARD]` — *Evidence:* One effect calls a function declared below it, working only because the effect runs after the render body completes. Several empty dependency lists mask functions recreated every render. *Why:* No error boundary means any render throw white-screens the app — the delivery mechanism for API-C1. The theme toggle's effect-set state makes the control flash in one frame late on every page. *Fix:* Add a boundary, correct the dependencies, and use lazy state initializers.
- [ ] **FE-M5 · Impure calls during render and an uncapped 3D render loop on the login page** — [NetworkGlobe.tsx:17-18](frontend/src/components/ui/NetworkGlobe.tsx#L17-L18), [:81](frontend/src/components/ui/NetworkGlobe.tsx#L81) · `[CARRY FORWARD]` — *Evidence:* Randomized geometry generated during render inside a memo; the canvas declares no frame-loop mode or pixel-ratio cap. *Why:* A memo is not a correctness guarantee, and under strict-mode double rendering the three-thousand-point cloud is generated twice. The scene then renders at full rate forever on an unauthenticated page — including on phones — while the layout renders it at twenty-percent opacity and non-interactive. Resource burn for a nearly invisible decoration. *Fix:* Move generation to a seeded initializer and cap the frame loop and pixel ratio.
- [ ] **FE-M6 · Index-based list keys over filtered and re-derived collections** — [GeneralReportPage.tsx:287](frontend/src/features/reports/components/GeneralReportPage.tsx#L287), [AutocompleteInput.tsx:53](frontend/src/components/ui/AutocompleteInput.tsx#L53) · `[CARRY FORWARD]` — *Evidence:* The report table keys on position while a stable identifier is present on the type and returned by the backend; the autocomplete keys on position over a list re-filtered each keystroke. *Why:* Changing a filter reshuffles positions and the framework reuses the wrong rows. *Fix:* Key on the stable identifier.
- [ ] **FE-M7 · Five forms have no validation, and one can submit an undefined required field** — [EquipmentPage.tsx:21-24](frontend/src/features/equipment/components/EquipmentPage.tsx#L21-L24), [:30-34](frontend/src/features/equipment/components/EquipmentPage.tsx#L30-L34), [:119-124](frontend/src/features/equipment/components/EquipmentPage.tsx#L119-L124), [:244-247](frontend/src/features/equipment/components/EquipmentPage.tsx#L244-L247), [VerificationForm.tsx:13-18](frontend/src/features/equipment/components/VerificationForm.tsx#L13-L18), [AdminPanel.tsx:79](frontend/src/features/dashboard/components/AdminPanel.tsx#L79) · `[CARRY FORWARD]` — *Evidence:* Only the login form uses the installed schema-validation and form libraries; every other form is raw state with disabled-button guards. The fault report resolves its name by lookup and passes the result through optionally. *Why:* If the fault list is stale relative to the selection, the field serializes away entirely and the request fails validation server-side, surfacing only as a generic alert. One numeric parse has no not-a-number check. *Fix:* Extend the existing schema-validation pattern to all forms.
- [ ] **FE-M8 · Bilingual interface with no internationalization, and fabricated marketing figures on the login page** — [LoginPage.tsx:90-92](frontend/src/features/auth/components/LoginPage.tsx#L90-L92), [:105-110](frontend/src/features/auth/components/LoginPage.tsx#L105-L110), [:164](frontend/src/features/auth/components/LoginPage.tsx#L164), [:171-173](frontend/src/features/auth/components/LoginPage.tsx#L171-L173), [:191-204](frontend/src/features/auth/components/LoginPage.tsx#L191-L204) · `[REDESIGN]` — *Evidence:* The application is right-to-left throughout, yet the login hero, navigation, and several component defaults are left-to-right English, with a bilingual submit button. Three navigation links are dead anchors. The footer presents invented performance statistics, and two badges assert encryption and transport security that a local deployment does not have. *Why:* Punctuation renders on the wrong side inside the right-to-left container; more importantly, an inventory system's login page makes false security and performance claims. *Fix:* Introduce a string catalogue; delete the fabricated statistics and unearned security badges.
- [ ] **FE-M9 · Broad accessibility failures** — [LoginPage.tsx:122-145](frontend/src/features/auth/components/LoginPage.tsx#L122-L145), four modal sites, [AdminPanel.tsx:125-131](frontend/src/features/dashboard/components/AdminPanel.tsx#L125-L131), [SearchableMultiSelect.tsx:67-71](frontend/src/components/ui/SearchableMultiSelect.tsx#L67-L71), [index.html:2](frontend/index.html#L2) · `[CARRY FORWARD]` — *Evidence:* The login form has no labels at all — placeholder-only, with no autocomplete hints, while the label component sits exported and unused. Four modals are plain containers with no dialog role, no modal marking, no focus trap or restoration, and no dismiss-on-escape. Icon-only buttons have no accessible names. Report filters and search results are click handlers on non-interactive containers with no keyboard path. The document declares English on an all-Hebrew right-to-left application, with a placeholder title and default icon. *Why:* The report filter interface is entirely mouse-only; password managers cannot fill the login form. *Fix:* Adopt the existing label component, add dialog semantics and focus management, and correct the document language.
- [ ] **FE-M10 · Six types re-declared inline instead of imported, two with the same name and incompatible shapes** — [AdminPanel.tsx:4-14](frontend/src/features/dashboard/components/AdminPanel.tsx#L4-L14), [MaintenancePage.tsx:9-18](frontend/src/features/maintenance/components/MaintenancePage.tsx#L9-L18), [GeneralReportPage.tsx:9-19](frontend/src/features/reports/components/GeneralReportPage.tsx#L9-L19), [auth.service.ts:8-11](frontend/src/services/auth.service.ts#L8-L11), others · `[REDESIGN]` — *Evidence:* Each shadows a shared type with differing fields or nullability. Two credential types share a name with incompatible shapes, and the shared one is used by nobody. *Why:* This is the mechanism by which API-H4 arose — the shared type was never corrected because nothing used it. *Fix:* One shared type module, generated per API-M2.
- [ ] **FE-M11 · Dead props and unreachable interface elements on live components** — [App.tsx:37](frontend/src/App.tsx#L37), [DashboardPage.tsx:27](frontend/src/features/dashboard/components/DashboardPage.tsx#L27), [DailyActivityTable.tsx:22](frontend/src/features/dashboard/components/DailyActivityTable.tsx#L22), [useDashboardData.ts:8](frontend/src/features/dashboard/hooks/useDashboardData.ts#L8) · `[CARRY FORWARD]` — *Evidence:* Close and logout handlers are passed and renamed to unused; the activity table's view-all callback is never supplied, so its button is unreachable; the dashboard hook computes a loading flag the page never destructures. *Why:* The admin panel has no way to close, there is no way to view activity beyond the first eight entries, and refreshes show no loading indicator at all. *Fix:* Wire the props or remove them and the interface they gate.
- [ ] **FE-M12 · The same logic reimplemented across components, divergently** — role labels in two places; status and compliance badge maps in **four**; reason labels in two; equipment-history fetching in two; statistic cards in two; debounced user search in **three**; date formatting in **five** · `[REDESIGN]` — *Evidence:* Each pair or group is near-identical with small, unexplained differences. Two role-label implementations both silently omit the same role, so a technician sees a raw identifier as their title. *Fix:* Extract shared presentation helpers; this also collapses much of FE-H5.
- [ ] **FE-M13 · Inconsistent optional-chaining in the report filter** — [GeneralReportPage.tsx:84-88](frontend/src/features/reports/components/GeneralReportPage.tsx#L84-L88) · `[CARRY FORWARD]` — *Evidence:* Four of five fields are dereferenced unguarded while the fifth is guarded, all declared non-optional. *Why:* Works only because the backend currently coalesces to empty strings; any change to return null crashes the entire report on the first keystroke. *Fix:* Guard consistently.
- [ ] **FE-M14 · Login discards the intended destination and leaves inconsistent state on partial failure** — [App.tsx:67-72](frontend/src/App.tsx#L67-L72), [:105](frontend/src/App.tsx#L105) · `[CARRY FORWARD]` — *Evidence:* The token is stored before the follow-up profile fetch; if that fetch fails, the success path never runs. The catch-all route discards the attempted path. *Why:* The user sees a login error while a valid token sits in storage — component state says logged out, storage says logged in, and refreshing logs them in. Deep links are lost on session expiry. *Fix:* Make login atomic and preserve the intended destination.

### Low

- [ ] **FE-L1 · Truthiness checks that hide a zero identifier and stale modal state** — [EquipmentPage.tsx:612](frontend/src/features/equipment/components/EquipmentPage.tsx#L612), [VerificationForm.tsx:13-18](frontend/src/features/equipment/components/VerificationForm.tsx#L13-L18) · `[CARRY FORWARD]` — *Evidence:* A nullable numeric is tested for truthiness; a modal seeds state from props with no remount key. *Why:* Benign only because identifiers start at one and only one modal opens at a time; a future "next item" control would show stale data. *Fix:* Explicit null checks and a remount key.
- [ ] **FE-L2 · A read-only checkbox driven by a parent click handler** — [SearchableMultiSelect.tsx:72-77](frontend/src/components/ui/SearchableMultiSelect.tsx#L72-L77) · `[CARRY FORWARD]` — *Evidence:* Marked read-only with the toggle on a wrapping container. *Why:* The control is not keyboard-operable, and the read-only flag exists only to silence the framework warning. *Fix:* Use a real change handler.
- [ ] **FE-L3 · Search feedback threshold inconsistent with its empty state; no refresh after save** — [AdminPanel.tsx:44](frontend/src/features/dashboard/components/AdminPanel.tsx#L44), [:144](frontend/src/features/dashboard/components/AdminPanel.tsx#L144), [:75-86](frontend/src/features/dashboard/components/AdminPanel.tsx#L75-L86) · `[CARRY FORWARD]` — *Evidence:* Search fires at two characters but the no-results message renders only at three; the save handler refetches nothing. *Why:* At exactly two characters with no matches the user sees a blank box and no feedback. *Fix:* Align the thresholds and refetch after mutation.
- [ ] **FE-L4 · A static greeting and a dead "see more" affordance** — [DashboardPage.tsx:66-71](frontend/src/features/dashboard/components/DashboardPage.tsx#L66-L71), [:175-182](frontend/src/features/dashboard/components/DashboardPage.tsx#L175-L182) · `[CARRY FORWARD]` — *Evidence:* The greeting is computed once at render; the overflow indicator is styled as interactive but is inert text. *Fix:* Recompute on an interval and make the indicator a real link.
- [ ] **FE-L5 · Pointless non-null assertions** — [main.tsx:9](frontend/src/main.tsx#L9), [NetworkGlobe.tsx:7-8](frontend/src/components/ui/NetworkGlobe.tsx#L7-L8) · `[CARRY FORWARD]` — *Evidence:* The mount-point lookup is asserted, and two refs are asserted then guarded anyway. *Why:* The mount assertion throws an opaque error if the document template changes. *Fix:* Explicit checks with clear messages.

---

## 5 · Infrastructure & Build

### Critical

- [V] **INF-C1 · The database is published on every host interface with weak, committed credentials** — [docker-compose.yml:5-11](docker-compose.yml#L5-L11), [:22](docker-compose.yml#L22) · `[REDESIGN]`
  **Evidence:** Literal placeholder-grade username and password, hardcoded twice, in a tracked file — and the database port is bound to all interfaces. Notably the signing key on the adjacent line *is* correctly interpolated from the environment, so this is inconsistency rather than oversight.
  **Why it matters:** Nothing outside the internal network needs that port; the backend reaches the database internally. This is an internet-reachable database with guessable credentials.
  **Fix:** Remove the port publication, interpolate all credentials from the environment, and restore the deleted environment template documenting them.

- [V] **INF-C2 · No continuous integration, and none since the revert** · `[CARRY FORWARD]`
  **Evidence:** No workflow directory exists. A working fifty-one-line pipeline — with a database service, a readiness healthcheck, linting, and the test run — was added and then removed by the revert. It was never restored. It is recoverable from history.
  **Why it matters:** Nothing enforces the linting, typechecking, or testing this document depends on. Meanwhile the contributing guide instructs contributors to await a status check that will never run.
  **Fix:** Restore the pipeline from history and extend it with a frontend job.

- [V] **INF-C3 · No tests exist, and 704 lines were destroyed by an over-broad revert** · `[CARRY FORWARD]`
  **Evidence:** Four test files — shared fixtures plus suites for hierarchy scoping, the permission matrix, and the export feature — were added *before* the reverted change and removed by it anyway. Two of nine reverted artifacts were restored; the tests were not. Verified: the fixtures already target the current package layout and use an in-memory database with dependency overrides.
  **Why it matters:** The two surviving suites cover precisely the two most broken subsystems in this document. This is the single highest-leverage recovery available.
  **Fix:** Restore from history and adapt.

### High

- [ ] **INF-H1 · The production entrypoint runs a development server with file watching** — [Dockerfile.backend:10](Dockerfile.backend#L10) · `[REDESIGN]`
  **Evidence:** The only container command enables auto-reload. There is no production variant, no multi-stage build, and no compose override. Worker count, proxy header handling, and forwarded-address trust are all unset.
  **Why it matters:** A file watcher runs in production, disabling multi-worker operation; combined with the repository bind-mount, any stray host file write restarts the live server. Missing proxy header handling means client addresses will be wrong behind a proxy — which matters directly for the rate limiting SEC-M3 requires.
  **Fix:** Split development and production stages; run a real server configuration in production.

- [ ] **INF-H2 · The frontend image ships the development server as production** — [frontend/Dockerfile:15](frontend/Dockerfile#L15), [:1](frontend/Dockerfile#L1), [:8](frontend/Dockerfile#L8) · `[REDESIGN]`
  **Evidence:** The command starts the development server bound to all interfaces. No production build step, no static server, no multi-stage split. The base runtime reached end-of-life, while the type definitions target a runtime six major versions newer. Dependencies are installed unlocked rather than from the lockfile. The compose file also enables filesystem polling, which pegs a processor core continuously.
  **Why it matters:** Unminified source and source maps ship to users, the hot-reload socket stays open, every console statement is live, and builds are not reproducible.
  **Fix:** Multi-stage build producing static assets served by a real server; install from the lockfile; update the base runtime.

- [ ] **INF-H3 · The entire repository is mounted over the application directory** — [docker-compose.yml:24-25](docker-compose.yml#L24-L25) · `[REDESIGN]`
  **Evidence:** The mount shadows everything the image build copied in.
  **Why it matters:** The built image is never what actually runs, so reproducibility is zero; the build-context exclusions are bypassed entirely at runtime, placing the environment file and version-control metadata inside the container. This is the runtime half of SEC-M15.
  **Fix:** Mount only source paths in development; mount nothing in production.

- [ ] **INF-H4 · Both containers run as the root user** — [Dockerfile.backend](Dockerfile.backend), [frontend/Dockerfile](frontend/Dockerfile) · `[CARRY FORWARD]`
  **Evidence:** Neither declares a user or creates an unprivileged account.
  **Why it matters:** Any code execution is immediately root inside the container — which, via INF-H3, has the host source tree mounted writable.
  **Fix:** Create and switch to an unprivileged user in both.

- [ ] **INF-H5 · Dependencies are unpinned across two known breaking boundaries** — [requirements.txt](requirements.txt) · `[CARRY FORWARD]`
  **Evidence:** Eight of nine packages carry no constraint; there is no lockfile and no hash pinning. The one pin freezes the password-hashing backend at a 2022 release.
  **Why it matters:** The unpinned ORM spans a major version that moved the declarative base import the code still uses; the unpinned web framework drags the validation library across its version-one-to-two boundary, and the schemas rely on a version-two-only setting that older versions **silently ignore** — breaking serialization with no error. The single pin works around a known incompatibility by freezing the *dependency* rather than the *cause*, while leaving the causing library unpinned above it, so the same break can reappear from the other side. That library has had no release since 2020.
  **Fix:** Pin everything with a lockfile, address the hashing incompatibility at its source, and declare the validation library explicitly rather than relying on it transitively.

- [ ] **INF-H6 · The frontend build context has no exclusions and copies host modules into the image** — [frontend/Dockerfile:10](frontend/Dockerfile#L10) · `[CARRY FORWARD]`
  **Evidence:** No exclusion file exists for the frontend context. The root exclusion file lists the module directory but governs only the root context, and the frontend service uses a different one.
  **Why it matters:** Host-platform native modules are copied over the freshly installed ones, producing platform-mismatched binaries and a heavily bloated image. The backend context received exactly this treatment in an earlier commit; the frontend never did.
  **Fix:** Add a frontend exclusion file mirroring the root one.

- [ ] **INF-H7 · Startup ordering has no readiness condition and no healthchecks exist** — [docker-compose.yml:19-20](docker-compose.yml#L19-L20) · `[CARRY FORWARD]`
  **Evidence:** The dependency declaration guarantees start order only, and no service declares a healthcheck. Correctness rests entirely on a hand-rolled sixty-second retry loop in application code.
  **Why it matters:** Nothing can tell whether the backend is serving or stuck retrying. Notably the reverted pipeline **did** configure a proper readiness check — the knowledge existed and was reverted away.
  **Fix:** Add healthchecks and a readiness condition; the retry loop then becomes a fallback rather than the mechanism.

- [ ] **INF-H8 · Floating image tags and no reproducible builds** — [Dockerfile.backend:1](Dockerfile.backend#L1), [frontend/Dockerfile:1](frontend/Dockerfile#L1), [docker-compose.yml:3](docker-compose.yml#L3) · `[CARRY FORWARD]`
  **Evidence:** All three base images use moving tags with no digest pinning. The backend uses the full base image rather than a slim variant.
  **Why it matters:** Two builds a week apart produce different systems. The full base image is roughly eight times larger with a correspondingly larger vulnerability surface, and its interpreter version reaches end-of-life in late 2026.
  **Fix:** Pin by digest and move to slim variants.

- [ ] **INF-H9 · A type-checking escape hatch hides a broken source file** — [tsconfig.app.json:15](frontend/tsconfig.app.json#L15) · `[CARRY FORWARD]`
  **Evidence:** The library-check skip also skips declaration files inside the source directory.
  **Why it matters:** The typecheck passes green while a source file imports a symbol that does not exist (FE-H3). The suppression is doing real work, which is why it cannot simply be switched off first.
  **Fix:** Delete the broken file, then disable the skip.

### Medium

- [ ] **INF-M1 · Linting is not type-aware and skips the generated tree** — [eslint.config.js:9](frontend/eslint.config.js#L9), [:19](frontend/eslint.config.js#L19) · `[CARRY FORWARD]` — *Evidence:* No project service is configured, no accessibility plugin is present, the generated directory is not ignored, and the language target trails the compiler target. *Why:* Type-aware rules — unhandled promises, misused promises — are exactly the class that would have caught FE-M14 and FE-M3, and cannot run at all. None of FE-M9 is detectable. *Fix:* Enable type-aware linting, add the accessibility plugin, ignore generated output.
- [ ] **INF-M2 · No typecheck, test, or format scripts, and no enforcement** — [package.json:6-12](frontend/package.json#L6-L12) · `[CARRY FORWARD]` — *Evidence:* Type checking exists only inside the build; the lint script is wired to nothing. *Fix:* Add the scripts and enforce them in the pipeline.
- [ ] **INF-M3 · The build has no hardening or chunking** — [vite.config.ts](frontend/vite.config.ts) · `[CARRY FORWARD]` — *Evidence:* Source maps are not disabled, console statements are not dropped, no manual chunking, and no development proxy. *Why:* The absent proxy is precisely why the hardcoded addresses in FE-H1 exist; the 3D and particle libraries all land in one entry chunk. *Fix:* Add a proxy, drop console output, and split vendor chunks.
- [ ] **INF-M4 · The environment variable is set at runtime but inlined at build time** — [docker-compose.yml:38](docker-compose.yml#L38) · `[CARRY FORWARD]` — *Evidence:* The build tool substitutes these at build time. *Why:* It works only because the container runs the development server (INF-H2). The moment a real build is introduced the variable goes inert and the application silently falls back to the hardcoded local address. *Fix:* Provide configuration at runtime rather than build time.
- [ ] **INF-M5 · Restart policy on one service only; a documented volume footgun left armed** — [docker-compose.yml:27](docker-compose.yml#L27), [:41](docker-compose.yml#L41) · `[CARRY FORWARD]` — *Evidence:* Only the backend restarts automatically. The anonymous module volume goes stale whenever a dependency is added — documented in the project's own notes with a manual workaround. *Why:* If the database dies the stack stays down while the backend retry-loops forever. The working tree currently adds three dependencies, so the footgun is armed right now. *Fix:* Restart policies on all services; replace the anonymous volume.
- [ ] **INF-M6 · Container hygiene flags absent** — [Dockerfile.backend](Dockerfile.backend) · `[CARRY FORWARD]` — *Evidence:* No healthcheck, no unbuffered output, no bytecode suppression. The build also copies the entire frontend source into the backend image. *Why:* Without unbuffered output the startup diagnostics never reach the container logs, so the crash-loop in INF-L1 is invisible. *Fix:* Add the flags and tighten the copy.
- [ ] **INF-M7 · Configured tooling is not installed** — [pyproject.toml](backend/pyproject.toml), [requirements.txt](requirements.txt) · `[CARRY FORWARD]` — *Evidence:* Both the linter and the test runner are configured; neither is a declared dependency. The test path setting points at a directory that does not exist. *Why:* Configuration for tooling nobody can run — which is why thirteen unused imports survive (DATA-L4). *Fix:* Declare both.
- [ ] **INF-M8 · Component-tooling configuration points at a nonexistent file and assumes left-to-right** — [components.json:7](frontend/components.json#L7), [:14](frontend/components.json#L14) · `[CARRY FORWARD]` — *Evidence:* Names a configuration file whose real extension differs; right-to-left is disabled. *Why:* Any future component added through the tool fails or arrives with wrong-direction spacing — which is why one page already carries a manual direction workaround. *Fix:* Correct both.
- [ ] **INF-M9 · Style configuration defines animations with no keyframes and duplicates hand-written ones** — [tailwind.config.cjs:57-62](frontend/tailwind.config.cjs#L57-L62) · `[CARRY FORWARD]` — *Evidence:* Two animations reference keyframes defined nowhere; two others duplicate hand-written stylesheet rules. *Why:* The referencing utilities emit broken rules. *Fix:* Define or remove them.
- [ ] **INF-M10 · Missing compiler strictness flags, worked around rather than satisfied** — [tsconfig.app.json:24-28](frontend/tsconfig.app.json#L24-L28) · `[CARRY FORWARD]` — *Evidence:* Unchecked index access, exact optional properties, implicit returns, and implicit overrides are all unset. The unused-parameter rule that *is* set is being evaded by underscore-prefixed renames, which the linter then flags anyway. *Fix:* Enable the remaining flags and delete the dead parameters rather than renaming them.
- [ ] **INF-M11 · The client generator is deprecated and unmaintained** — [package.json:48](frontend/package.json#L48) · `[ABANDON]` — *Evidence:* Superseded by a maintained successor. *Why:* It is the source of the dead, stale, server-library-importing client in FE-H2 and API-H2. *Fix:* Remove it along with the generated tree.

### Low

- [ ] **INF-L1 · A missing signing key produces an opaque crash-loop** — [docker-compose.yml:23](docker-compose.yml#L23), [security.py:14-15](backend/security.py#L14-L15) · `[CARRY FORWARD]` — *Evidence:* With no environment file present the substitution yields an empty string and the application raises at import. *Why:* Fails closed, which is correct — but with an automatic restart policy and no healthcheck the container loops forever with no visible diagnosis. *Fix:* Add a healthcheck and a clear startup error.
- [ ] **INF-L2 · Build-context exclusions reference files that no longer exist** — [.dockerignore:21-29](.dockerignore#L21-L29) · `[CARRY FORWARD]` — *Evidence:* Lists a workflow directory and a license file, both deleted by the revert. *Why:* Harmless, but it is evidence the file has drifted from reality. *Fix:* Refresh once the revert damage is repaired.
- [ ] **INF-L3 · No runtime or package-manager version declarations** — [package.json](frontend/package.json) · `[CARRY FORWARD]` — *Evidence:* No engine field, no package-manager field, no dependency overrides. *Why:* Nothing prevents a contributor building on an incompatible runtime. *Fix:* Declare both.

---

## 6 · Docs & Hygiene

### High

- [ ] **DOC-H1 · The system map documents a function deleted in the current commit** — [SYSTEM_MAP.md:105](SYSTEM_MAP.md#L105), [:31](SYSTEM_MAP.md#L31) · `[CARRY FORWARD]`
  **Evidence:** Two separate passages attribute the access-control filter to the security module. That helper was removed in the head commit, and the module now contains only password and token functions. The filter actually lives inlined in two routers and is duplicated between them (DATA-H9).
  **Why it matters:** The document is the project's stated entry point and opens by instructing the reader to consult it before touching anything. It points them at the wrong file for the single most security-critical piece of logic — and the removal commit did not update it.
  **Fix:** Correct both passages, and adopt a rule that structural commits update the map in the same commit.

- [ ] **DOC-H2 · The equipment endpoint table is wrong in verb, path, and trailing slash** — [SYSTEM_MAP.md:176-184](SYSTEM_MAP.md#L176-L184) · `[CARRY FORWARD]`
  **Evidence:** One documented path omits its real suffix, one omits a significant trailing slash, and one documents the wrong HTTP verb entirely.
  **Why it matters:** Anyone coding against the documented contract gets not-found and method-not-allowed responses. This is the documentation-side counterpart to the entire Contract Drift section.
  **Fix:** Regenerate the endpoint table from the live specification rather than maintaining it by hand.

- [ ] **DOC-H3 · The contributing guide instructs contributors to run tests and await a status check that do not exist** — [CONTRIBUTING.md:63](CONTRIBUTING.md#L63), [:45](CONTRIBUTING.md#L45) · `[CARRY FORWARD]`
  **Evidence:** It directs contributors to run the test suite and to wait for the pipeline's green check. Neither exists — both were removed by the revert, and this file is one of only two artifacts that *were* restored.
  **Why it matters:** The restored documentation now describes the reverted world. A contributor following it waits forever for a check that will never run, having run a command that reports no tests.
  **Fix:** Resolve by restoring the tests and pipeline (INF-C2, INF-C3), which makes the guide true again rather than requiring it to be weakened.

- [ ] **DOC-H4 · No root readme and no license; the frontend readme is untouched scaffolding** — [frontend/README.md](frontend/README.md) · `[CARRY FORWARD]`
  **Evidence:** A sixty-four-line root readme and a license both existed before the revert and were never restored. The only readme present is the unmodified framework template describing configuration the project does not use.
  **Why it matters:** A newcomer's entry point is a twenty-four-kilobyte reference document that opens "read this before touching anything" — and which, per DOC-H1 and DOC-H2, is wrong in several load-bearing places.
  **Fix:** Restore both from history and update.

- [ ] **DOC-H5 · A stale generated API specification is tracked in version control** — [frontend/openapi.json](frontend/openapi.json) · `[ABANDON]`
  **Evidence:** A tracked, generated, 775-line artifact that is eighteen endpoints and one API version behind. See API-H1.
  **Why it matters:** It looks authoritative and is consulted as such. Regenerating the client from it propagates the staleness rather than fixing it.
  **Fix:** Untrack it; generate in the pipeline and fail on drift.

### Medium

- [ ] **DOC-M1 · Six further false claims in the system map** — [SYSTEM_MAP.md:29](SYSTEM_MAP.md#L29), [:96](SYSTEM_MAP.md#L96), [:331](SYSTEM_MAP.md#L331), [:43-91](SYSTEM_MAP.md#L43-L91), [:25](SYSTEM_MAP.md#L25), [:324](SYSTEM_MAP.md#L324) · `[CARRY FORWARD]` — *Evidence:* Claims thirteen tables where there are twelve — contradicting its own table listing twelve; documents an environment template deleted by the revert; describes a frontend behavior whose call site and state variable no longer exist anywhere; omits four real source directories including both the generated client and the second HTTP client; names the wrong repository root and hardcodes a stale absolute path under the wrong parent directory. *Why:* A reader following the file tree would never learn there are four HTTP layers — the root cause of FE-H1. *Fix:* Rewrite against the current tree; prefer generated sections over hand-maintained ones.
- [ ] **DOC-M2 · Both backend commands in the personal notes file are broken** — [notes_self.txt:3-4](notes_self.txt#L3-L4) · `[ABANDON]` — *Evidence:* One invokes the seed script directly, which now fails on relative imports since the package restructure; the other targets a module deleted several commits ago. The system map has the correct form for one of them, so the two documents disagree. The file was reintroduced by the revert and never corrected. *Why:* It is also the file that documents the destructive seed command from DATA-C2. *Fix:* Delete it; fold anything worth keeping into the restored readme.
- [ ] **DOC-M3 · The linter is documented as mandatory but enforced nowhere** — [CONTRIBUTING.md:58](CONTRIBUTING.md#L58) · `[CARRY FORWARD]` — *Evidence:* The guide states the project uses it for linting and formatting; it is configured but not installed (INF-M7), with no commit hook and no pipeline step. *Fix:* Install it and enforce it in the pipeline and a commit hook.
- [ ] **DOC-M4 · No editor configuration, and formatting has visibly drifted** — repository-wide · `[CARRY FORWARD]` — *Evidence:* No such file has ever existed. The backend carries trailing whitespace throughout, while frontend source and frontend configuration use different indentation and semicolon conventions. The reverted commit hooks included whitespace and end-of-file fixers. *Fix:* Add an editor configuration and restore the commit hooks.
- [ ] **DOC-M5 · Ignore rules are incomplete and have drifted from the build-context rules** — [.gitignore](.gitignore) · `[CARRY FORWARD]` — *Evidence:* Missing build output, linter cache, coverage output, log files, and the agent configuration directory currently untracked and unignored. The build-context file *does* list two of these — the two ignore files have diverged. The reverted commit had expanded this file. *Why:* The guide's own example instructs staging everything, which is how build artifacts get committed. *Fix:* Expand and keep the two files aligned.
- [ ] **DOC-M6 · The working tree is dirty with a change that should be discarded** — [package.json](frontend/package.json), lockfile, [ParticlesBackground.tsx](frontend/src/components/ui/ParticlesBackground.tsx) · `[ABANDON]` — *Evidence:* Three uncommitted files adding three dependencies to support a component nothing imports. *Why:* Per FE-C1, deleting the component is the correct resolution — so this work should be discarded rather than committed. *Fix:* Discard, delete the component, commit that.

### Low

- [ ] **DOC-L1 · Stale remote branches still carry the lost infrastructure** · `[CARRY FORWARD]` — *Evidence:* Two remote branches remain, one of them the reverted change containing the pipeline, license, readme, and commit hooks the repository now lacks. *Why:* Worth keeping until the recovery in INF-C2 and INF-C3 is complete. *Fix:* Delete after recovery.
- [ ] **DOC-L2 · The backend project file declares no project metadata** — [backend/pyproject.toml](backend/pyproject.toml) · `[CARRY FORWARD]` — *Evidence:* Tool configuration only — no build system, no metadata, no dependency declaration; those live in the unpinned requirements file. *Fix:* Consolidate dependency declaration here.
- [ ] **DOC-L3 · Feature claims that are false in practice** — [SYSTEM_MAP.md:266](SYSTEM_MAP.md#L266), [:72](SYSTEM_MAP.md#L72), [:188](SYSTEM_MAP.md#L188), [:279](SYSTEM_MAP.md#L279) · `[CARRY FORWARD]` — *Evidence:* The admin page is documented as offering three capabilities, of which one works (API-C1, API-C2); export is documented as working (API-C3); the ticket filter is described without naming its parameter — the exact ambiguity that produced API-H5; auto-reload is presented as the deployment story (INF-H1). *Fix:* Correct alongside DOC-M1.

---

## Recoverable from git history

Verified present and retrievable. This is the highest-leverage work in the document:

| Artifact | Size | State |
|---|---|---|
| Test fixtures | 318 lines | **Already targets the current package layout**; in-memory database with dependency overrides |
| Hierarchy-scoping suite | 88 lines | Covers SEC-H1 and SEC-H2 directly; needs one import-path adaptation |
| Permission-matrix suite | 185 lines | Covers SEC-H3 and SEC-H4 directly; needs the same adaptation |
| Export-feature suite | 113 lines | Tests the endpoint from API-C3 — restore as a *failing* test or drop with a recorded decision |
| Continuous integration pipeline | 51 lines | Database service, readiness healthcheck, linting, test run |
| Commit hooks | 14 lines | Includes a large-file check that would have blocked the committed database binary the same change added |
| Root readme, license, environment template | 74 lines | Never restored |

**Expect the recovered suites to fail on first run.** They encode pre-revert behavior that the fixes in this document deliberately change. That failure output is the most valuable diagnostic available and should be recorded here rather than papered over.

---

## The primary rewrite decision

Every entry tagged `[REDESIGN]` in the Security section traces back to one question this document deliberately does not answer:

**The hierarchy model is the system's core security dimension, and it is inert for all data created through the API.** It is elaborately modelled, seeded, documented, and displayed — and written by no application code path. Six of its permission flags are checked by no router, and a hardcoded name list overrides the ones that are.

Two options:

1. **Repair in place** — populate the hierarchy on user and equipment creation, backfill existing rows, anchor the prefix matching, and enforce the six orphaned permissions. Lower cost; retains a materialized-path design whose prefix semantics already produced a cross-unit leak.
2. **Replace** — model units as real rows with real relationships and derive scoping relationally. Higher cost; eliminates the entire class of defect represented by SEC-H1, SEC-H2, and DATA-M16.

**This decision should be made before any `[REDESIGN]` item is worked**, because it determines whether those items are fixed or discarded.

---

## Method and confidence

- **Directly verified by execution:** the toolchain results in the table above; the git-history recovery (each artifact retrieved and inspected); the dead-code import checks; the clean-clone build failure in FE-C1.
- **Derived from code reading and cross-checked across three independent audits:** the backend findings. The backend has no runnable environment in this workspace — its dependencies are not installed — so no backend defect here was reproduced against a live server.
- **Sequencing constraints, not merely findings:** DATA-H10 (the application cannot be imported without a database) blocks the entire recovered test suite, and DATA-C1 (no migration tooling) blocks every schema fix. These two gate most of the rest of this document.
